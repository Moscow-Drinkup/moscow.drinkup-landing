"use strict";
const utils_1 = require("markdown-it/lib/common/utils");
const utils_2 = require("../utils");
const inlineCode = (md, options) => {
    var _a;
    const generateID = (_a = options.generateID) !== null && _a !== void 0 ? _a : utils_2.generateID;
    md.renderer.rules.code_inline = function (tokens, idx) {
        const id = generateID('inline-code-id');
        return `<code class="yfm-clipboard-inline-code" role="button" tabindex='0' id="${id}">${(0, utils_1.escapeHtml)(tokens[idx].content)}</code>`;
    };
};
module.exports = inlineCode;
//# sourceMappingURL=index.js.map