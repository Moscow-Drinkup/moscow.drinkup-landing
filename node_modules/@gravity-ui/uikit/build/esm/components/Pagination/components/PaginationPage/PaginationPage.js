'use client';
import { createElement as _createElement } from "react";
import { jsx as _jsx } from "react/jsx-runtime";
import { Button } from "../../../Button/index.js";
import { block } from "../../../utils/cn.js";
import { getPaginationPageQa } from "../../constants.js";
import { buildComponentProps } from "../../utils.js";
import "./PaginationPage.css";
const b = block('pagination-page');
export const PaginationPage = ({ item, size, pageSize, className, onUpdate, pageComponent, getPageProps, }) => {
    const qa = getPaginationPageQa(item.page);
    if (item.simple) {
        return (_jsx("div", { "data-qa": qa, className: b('simple', { size }, className), children: item.page }));
    }
    const view = item.current ? 'normal' : 'flat';
    const componentProps = buildComponentProps({
        component: pageComponent,
        item,
        getPageProps,
        page: item.page,
        pageSize,
        onUpdate,
    });
    return (_createElement(Button, { ...componentProps, size: size, key: view, view: view, selected: item.current, className: className, qa: qa }, item.page));
};
//# sourceMappingURL=PaginationPage.js.map
