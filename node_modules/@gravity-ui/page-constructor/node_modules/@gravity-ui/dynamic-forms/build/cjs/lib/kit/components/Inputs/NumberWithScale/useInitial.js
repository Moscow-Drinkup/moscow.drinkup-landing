"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useInitial = void 0;
const tslib_1 = require("tslib");
const react_1 = tslib_1.__importDefault(require("react"));
const utils_1 = require("../../../utils");
const useInitial = (value, spec, deps = []) => {
    const initial = react_1.default.useMemo(() => {
        const { defaultType, viewType, scale } = spec.viewSpec.sizeParams;
        const correctValue = (0, utils_1.divide)(value, value) !== null;
        let initialType = defaultType;
        let initialValue = value;
        if (viewType) {
            if (correctValue) {
                if (BigInt(scale[viewType].factor) > BigInt(scale[initialType].factor)) {
                    initialValue = (0, utils_1.divide)(value, (0, utils_1.divide)(scale[viewType].factor, scale[initialType].factor), 2);
                }
                else {
                    initialValue = (0, utils_1.multiply)(value, (0, utils_1.divide)(scale[initialType].factor, scale[viewType].factor), 2);
                }
            }
            initialType = viewType;
        }
        if (correctValue) {
            Object.keys(scale).forEach((key) => {
                if (BigInt(scale[key].factor) > BigInt(scale[initialType].factor)) {
                    const candidate = (0, utils_1.divide)(value, (0, utils_1.divide)(scale[key].factor, scale[defaultType].factor), 2);
                    if (candidate !== null && BigInt(candidate.split('.')[0]) >= BigInt(1)) {
                        initialType = key;
                        initialValue = candidate;
                    }
                }
            });
        }
        return { initialType, initialValue };
    }, deps);
    return initial;
};
exports.useInitial = useInitial;
