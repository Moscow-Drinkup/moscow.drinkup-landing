"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ArrayBase = void 0;
const tslib_1 = require("tslib");
const react_1 = tslib_1.__importDefault(require("react"));
const icons_1 = require("@gravity-ui/icons");
const uikit_1 = require("@gravity-ui/uikit");
const core_1 = require("../../../core");
const components_1 = require("../../components");
const Component = ({ controlProps, input, schema }) => {
    const { name, onBlur, onChange, onFocus, value } = input;
    const addButton = react_1.default.useMemo(() => {
        const itemsSchema = schema.items;
        if (Array.isArray(itemsSchema)) {
            return null;
        }
        const onClick = () => {
            onFocus();
            onChange([...(value || []), itemsSchema === null || itemsSchema === void 0 ? void 0 : itemsSchema.default]);
            onBlur();
        };
        return (react_1.default.createElement(uikit_1.Button, { onClick: onClick, disabled: controlProps.disabled || schema.readOnly, qa: `${name}-add-button` },
            react_1.default.createElement(uikit_1.Icon, { data: icons_1.Plus, size: 14 }),
            controlProps.addButtonText || null));
    }, [
        controlProps.addButtonText,
        controlProps.disabled,
        name,
        onBlur,
        onChange,
        onFocus,
        schema.items,
        schema.readOnly,
        value,
    ]);
    const items = react_1.default.useMemo(() => {
        const itemsSchema = schema.items;
        if (Array.isArray(itemsSchema)) {
            return itemsSchema.map((item, index) => (react_1.default.createElement(core_1.Entity, { name: `${name}[${index}]`, schema: item, key: index })));
        }
        return new Array(value === null || value === void 0 ? void 0 : value.length)
            .fill(null)
            .map((_, index) => (react_1.default.createElement(core_1.Entity, { name: `${name}[${index}]`, schema: itemsSchema, key: index })));
    }, [name, schema.items, value === null || value === void 0 ? void 0 : value.length]);
    return (react_1.default.createElement(components_1.ControlContainer, { stretch: "by-child", gap: 4 },
        react_1.default.createElement(uikit_1.Flex, { direction: "column" }, items),
        addButton));
};
exports.ArrayBase = react_1.default.memo(Component);
