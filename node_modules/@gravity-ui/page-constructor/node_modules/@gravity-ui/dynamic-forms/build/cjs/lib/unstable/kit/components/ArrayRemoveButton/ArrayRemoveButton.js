"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ArrayRemoveButton = void 0;
const tslib_1 = require("tslib");
const react_1 = tslib_1.__importDefault(require("react"));
const icons_1 = require("@gravity-ui/icons");
const uikit_1 = require("@gravity-ui/uikit");
const react_final_form_1 = require("react-final-form");
const utils_1 = require("../../utils");
const ArrayRemoveButton = ({ name, onClick }) => {
    const form = (0, react_final_form_1.useForm)();
    const [ready, setReady] = react_1.default.useState(false);
    const arrayItem = (0, utils_1.isArrayItem)(name);
    const tupleItem = (0, utils_1.isTupleItem)(name, form);
    const removeItem = react_1.default.useCallback((event) => {
        var _a;
        onClick === null || onClick === void 0 ? void 0 : onClick(event);
        const parentName = (0, utils_1.getArrayItemParentName)(name);
        const parentValue = (_a = form.getFieldState(parentName)) === null || _a === void 0 ? void 0 : _a.value;
        const index = Number((0, utils_1.getArrayItemIndex)(name));
        if (Array.isArray(parentValue)) {
            form.focus(parentName);
            form.change(parentName, parentValue.filter((_, i) => i !== index));
            form.blur(parentName);
        }
    }, [form, name, onClick]);
    react_1.default.useLayoutEffect(() => {
        setReady(true);
    }, []);
    if (ready && arrayItem && !tupleItem) {
        return (react_1.default.createElement(uikit_1.Button, { view: "flat-secondary", onClick: removeItem, qa: `${name}-remove-button` },
            react_1.default.createElement(uikit_1.Icon, { data: icons_1.TrashBin, size: 16 })));
    }
    return null;
};
exports.ArrayRemoveButton = ArrayRemoveButton;
