import React from 'react';
import { Checkbox as UIKitCheckbox, } from '@gravity-ui/uikit';
import { ControlContainer } from '../../components';
import { block, getBooleanValidationState } from '../../utils';
import './Checkbox.css';
const b = block('checkbox');
const Component = ({ controlProps, input, meta, schema, }) => {
    const { name, onBlur, onChange, onFocus, value } = input;
    const onUpdate = React.useCallback((value) => {
        onFocus();
        onChange(value);
        onBlur();
    }, [onBlur, onChange, onFocus]);
    return (React.createElement(ControlContainer, { stretch: "fit", className: b({ error: getBooleanValidationState(meta) }) },
        React.createElement(UIKitCheckbox, Object.assign({ disabled: schema.readOnly }, controlProps, { checked: value !== null && value !== void 0 ? value : false, onFocus: onFocus, onBlur: onBlur, onUpdate: onUpdate, qa: name }))));
};
export const Checkbox = React.memo(Component);
