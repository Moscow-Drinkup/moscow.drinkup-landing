import React from 'react';
import { useField } from 'react-final-form';
import { SchemaRendererMode } from '../constants';
import { useEntitiesState } from '../useSchemaRenderer';
import { getSchemaBySchemaPath, smartMerge } from '../utils';
import { getRenderKit } from './utils';
const EntityComponent = ({ modeOverride, name, schema: schemaProps = {}, schemaOverride, }) => {
    var _a, _b;
    const { config, error, headName, mode: modeState, rootSchema } = useEntitiesState(name);
    const getAccumulatedSchema = React.useCallback((schema, root, override) => {
        let accumulatedSchema = schema;
        if (override) {
            accumulatedSchema = smartMerge(accumulatedSchema, override);
        }
        if (accumulatedSchema.$ref && root) {
            const schemaByRef = getSchemaBySchemaPath(accumulatedSchema.$ref, root);
            if (schemaByRef) {
                accumulatedSchema = smartMerge(accumulatedSchema, schemaByRef);
            }
        }
        return accumulatedSchema;
    }, []);
    const options = React.useMemo(() => {
        const data = { headName, schema: schemaProps };
        const accumulatedSchema = getAccumulatedSchema(schemaProps, rootSchema, schemaOverride);
        return {
            data,
            defaultValue: accumulatedSchema.default,
            subscription: {
                data: true,
                submitFailed: true,
                touched: true,
                validating: true,
                value: true,
            },
        };
    }, [headName, getAccumulatedSchema, rootSchema, schemaOverride, schemaProps]);
    const field = useField(name, options);
    const meta = React.useMemo(() => (Object.assign(Object.assign({}, field.meta), { error })), [field.meta, error]);
    const schema = React.useMemo(() => { var _a; return getAccumulatedSchema((_a = field.meta.data) === null || _a === void 0 ? void 0 : _a.schema, rootSchema, schemaOverride); }, [(_a = field.meta.data) === null || _a === void 0 ? void 0 : _a.schema, schemaOverride, getAccumulatedSchema, rootSchema]);
    const mode = modeOverride || ((_b = field.meta.data) === null || _b === void 0 ? void 0 : _b.mode) || modeState;
    const renderKit = React.useMemo(() => getRenderKit({ config, schema }), [config, schema]);
    let content = null;
    if (mode === SchemaRendererMode.Form) {
        const formKit = renderKit[SchemaRendererMode.Form];
        if (formKit.Component) {
            if (formKit.independent) {
                content = (React.createElement(formKit.Component, { input: field.input, meta: meta, schema: schema, controlProps: formKit.props, Wrapper: formKit.Wrapper, wrapperProps: formKit.wrapperProps }));
            }
            else {
                content = (React.createElement(formKit.Component, { input: field.input, meta: meta, schema: schema, controlProps: formKit.props }));
                if (formKit.Wrapper) {
                    content = (React.createElement(formKit.Wrapper, { input: field.input, meta: meta, schema: schema, wrapperProps: formKit.wrapperProps }, content));
                }
            }
        }
    }
    if (mode === SchemaRendererMode.Overview) {
        const overviewKit = renderKit[SchemaRendererMode.Overview];
        if (overviewKit.Component) {
            if (overviewKit.independent) {
                content = (React.createElement(overviewKit.Component, { input: field.input, meta: meta, schema: schema, viewProps: overviewKit.props, Wrapper: overviewKit.Wrapper, wrapperProps: overviewKit.wrapperProps }));
            }
            else {
                content = (React.createElement(overviewKit.Component, { input: field.input, meta: meta, schema: schema, viewProps: overviewKit.props }));
                if (overviewKit.Wrapper) {
                    content = (React.createElement(overviewKit.Wrapper, { input: field.input, meta: meta, schema: schema, wrapperProps: overviewKit.wrapperProps }, content));
                }
            }
        }
    }
    return content;
};
export const Entity = React.memo(EntityComponent);
