import { __rest } from "tslib";
import React from 'react';
import { unstable_ColorPicker as UIKitColorPicker, } from '@gravity-ui/uikit/unstable';
import { ControlContainer } from '../../components';
import { block, getBooleanValidationState } from '../../utils';
import './ColorPicker.css';
const b = block('color-picker');
const Component = ({ controlProps, input, meta, schema, }) => {
    const { name, onBlur, onChange, onFocus, value } = input;
    const { onOpenChange: onOpenChangeProps } = controlProps, restControlProps = __rest(controlProps, ["onOpenChange"]);
    const onOpenChange = React.useCallback((open) => {
        onOpenChangeProps === null || onOpenChangeProps === void 0 ? void 0 : onOpenChangeProps(open);
        if (open) {
            onFocus();
        }
        else {
            onBlur();
        }
    }, [onBlur, onFocus, onOpenChangeProps]);
    return (React.createElement(ControlContainer, { stretch: "max", className: b({ error: getBooleanValidationState(meta) }) },
        React.createElement(UIKitColorPicker, Object.assign({ disabled: schema.readOnly }, restControlProps, { value: value !== null && value !== void 0 ? value : '', onUpdate: onChange, onOpenChange: onOpenChange, "data-qa": name }))));
};
export const ColorPicker = React.memo(Component);
