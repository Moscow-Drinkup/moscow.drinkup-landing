import React from 'react';
import { Entity } from '../Entity';
import { useSchemaRenderer } from '../useSchemaRenderer';
const SchemaRendererComponent = (props) => {
    const { schema } = useSchemaRenderer(props);
    return schema ? React.createElement(Entity, { name: props.name, schema: schema }) : null;
};
export const SchemaRenderer = React.memo(SchemaRendererComponent);
