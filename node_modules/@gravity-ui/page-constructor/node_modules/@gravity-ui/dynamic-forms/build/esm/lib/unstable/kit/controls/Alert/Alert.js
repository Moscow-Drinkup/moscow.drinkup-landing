import { __rest } from "tslib";
import React from 'react';
import * as icons from '@gravity-ui/icons';
import { Icon, Alert as UIKitAlert, } from '@gravity-ui/uikit';
import { ControlContainer } from '../../components';
const Component = ({ controlProps, schema }) => {
    const { iconName, iconProps, message } = controlProps, controlRestProps = __rest(controlProps, ["iconName", "iconProps", "message"]);
    const icon = React.useMemo(() => iconName && icons[iconName] ? (React.createElement(Icon, Object.assign({ data: icons[iconName] }, iconProps))) : undefined, [iconName, iconProps]);
    const msg = React.useMemo(() => {
        if (message) {
            if (typeof message === 'string') {
                return React.createElement("span", { dangerouslySetInnerHTML: { __html: message } });
            }
            return message;
        }
        if (schema.description) {
            return React.createElement("span", { dangerouslySetInnerHTML: { __html: schema.description } });
        }
        return undefined;
    }, [message, schema.description]);
    return (React.createElement(ControlContainer, { stretch: "fit" },
        React.createElement(UIKitAlert, Object.assign({ icon: icon, message: msg }, controlRestProps))));
};
export const Alert = React.memo(Component);
