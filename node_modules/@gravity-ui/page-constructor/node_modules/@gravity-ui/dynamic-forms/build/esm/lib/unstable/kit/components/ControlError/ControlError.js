import React from 'react';
import { Text } from '@gravity-ui/uikit';
import { block } from '../../utils';
import './ControlError.css';
const b = block('control-error');
export const ControlError = ({ errorMessage, validationState }) => {
    if (validationState === 'invalid' && errorMessage) {
        return (React.createElement(Text, { className: b(), color: "danger" }, errorMessage));
    }
    return null;
};
