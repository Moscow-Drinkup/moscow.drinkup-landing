"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useTokenizedInputInfo = void 0;
const React = __importStar(require("react"));
const uikit_1 = require("@gravity-ui/uikit");
const isEqual_1 = __importDefault(require("lodash/isEqual.js"));
const undoredo_manager_1 = require("../undoredo-manager.js");
const utils_1 = require("../utils.js");
const useTokenizedInputInfo = ({ defaultTokens = [], isEditable = true, isClearable = true, transformTokens = utils_1.defaultTransformTokens, validateToken = utils_1.defaultValidateToken, formatToken, tokens: externalTokens, tokenErrors, fields, placeholder, className, size = 'm', onChange, }) => {
    const validateTokens = React.useCallback((t) => t.map((token) => ({
        ...token,
        errors: validateToken ? validateToken(token.value) : undefined,
    })), [validateToken]);
    const [tokens, setTokens] = React.useState(validateTokens(transformTokens(externalTokens)));
    const wrapperRef = React.useRef(null);
    const tokensRef = React.useRef(tokens);
    const undoRedoManager = React.useRef(new undoredo_manager_1.UndoRedoManager(validateTokens(transformTokens(externalTokens))));
    React.useEffect(() => {
        if (!(0, isEqual_1.default)((0, utils_1.getValuesFromTokens)(tokens.filter((t) => t.kind !== 'new')), externalTokens)) {
            const newTokens = validateTokens(transformTokens(externalTokens));
            tokensRef.current = newTokens;
            setTokens(newTokens);
            undoRedoManager.current.init(newTokens);
        }
        // We only want to sync the internal state when externalTokens change from props,
        // to avoid infinite loops if internal tokens or transformation functions change on every render.
        // eslint-disable-next-line react-hooks/exhaustive-deps
    }, [externalTokens]);
    const onChangeTokens = React.useCallback((newTokens, forceRewriteHistory = false) => {
        tokensRef.current = newTokens;
        setTokens(newTokens);
        undoRedoManager.current.update(newTokens, forceRewriteHistory);
        return newTokens;
    }, []);
    const onChangeToken = React.useCallback((idx, newValue) => {
        const prevTokens = tokensRef.current;
        if (idx >= prevTokens.length) {
            const newTokens = [
                ...prevTokens,
                {
                    id: `tokenId${(0, uikit_1.getUniqId)()}`,
                    kind: 'new',
                    value: {
                        ...(0, utils_1.getDefaultTokenValue)(fields),
                        ...newValue,
                    },
                },
            ];
            return onChangeTokens(newTokens);
        }
        else {
            const newTokens = prevTokens
                .map((t, i) => i === idx
                ? {
                    ...t,
                    value: { ...t.value, ...newValue },
                    errors: undefined,
                }
                : { ...t })
                .filter((t) => Object.values(t.value).some((v) => v.trim()));
            return onChangeTokens(newTokens);
        }
    }, [fields, onChangeTokens]);
    const onApplyChanges = React.useCallback((currentTokens = false) => {
        const transformedTokens = tokensRef.current.map((t) => {
            const { value, options } = transformTokens([t.value])[0];
            return { ...t, value, options };
        });
        const newTokens = (0, utils_1.removeEmptyTokens)(transformedTokens)
            .map((t) => {
            // apply changes only to existing tokens (not the in-progress new token)
            if (currentTokens && t.kind === 'new') {
                return undefined;
            }
            const formattedValue = formatToken?.(t.value) ?? t.value;
            return {
                ...t,
                kind: 'regular',
                value: formattedValue,
                errors: validateToken ? validateToken(formattedValue) : undefined,
            };
        })
            .filter(Boolean);
        onChange((0, utils_1.getValuesFromTokens)(newTokens));
        onChangeTokens(newTokens, true);
    }, [formatToken, onChange, onChangeTokens, transformTokens, validateToken]);
    const onRemoveToken = React.useCallback((idx) => {
        if (idx < 0 || idx >= tokensRef.current.length) {
            return tokensRef.current;
        }
        const newTokens = tokensRef.current.filter((_, i) => i !== idx);
        onChange((0, utils_1.getValuesFromTokens)(newTokens));
        return onChangeTokens(newTokens);
    }, [onChange, onChangeTokens]);
    const onClearInput = React.useCallback(() => {
        const newTokens = transformTokens(defaultTokens);
        onChange(defaultTokens);
        return onChangeTokens(newTokens);
    }, [defaultTokens, onChange, onChangeTokens, transformTokens]);
    const onUndo = React.useCallback(() => {
        const newTokens = undoRedoManager.current.undo();
        tokensRef.current = newTokens;
        setTokens(newTokens);
        onChange((0, utils_1.getValuesFromTokens)((0, utils_1.removeEmptyTokens)(newTokens.filter((t) => t.kind !== 'new'))));
        return newTokens;
    }, [onChange]);
    const onRedo = React.useCallback(() => {
        const newTokens = undoRedoManager.current.redo();
        tokensRef.current = newTokens;
        setTokens(newTokens);
        onChange((0, utils_1.getValuesFromTokens)((0, utils_1.removeEmptyTokens)(newTokens.filter((t) => t.kind !== 'new'))));
        return newTokens;
    }, [onChange]);
    const shouldRenderClearButton = React.useMemo(() => isClearable && externalTokens.length !== defaultTokens.length && isEditable, [isClearable, externalTokens.length, defaultTokens.length, isEditable]);
    const displayedTokens = React.useMemo(() => {
        if (!tokenErrors)
            return tokens;
        return tokens.map((token, i) => {
            if (token.kind === 'new')
                return token;
            const externalError = tokenErrors[i];
            if (!externalError)
                return token;
            return { ...token, errors: { ...token.errors, ...externalError } };
        });
    }, [tokens, tokenErrors]);
    return React.useMemo(() => ({
        state: {
            tokens: displayedTokens,
            wrapperRef,
            defaultTokens,
            fields,
            isEditable,
            isClearable: shouldRenderClearButton,
            placeholder: isEditable ? placeholder : undefined,
            className,
            size,
        },
        callbacks: {
            onApplyChanges,
            onChangeToken,
            onChangeTokens,
            onRemoveToken,
            onClearInput,
            onUndo,
            onRedo,
        },
    }), [
        defaultTokens,
        fields,
        isEditable,
        shouldRenderClearButton,
        placeholder,
        className,
        size,
        onApplyChanges,
        onChangeToken,
        onChangeTokens,
        onRemoveToken,
        onClearInput,
        onUndo,
        onRedo,
        displayedTokens,
    ]);
};
exports.useTokenizedInputInfo = useTokenizedInputInfo;
//# sourceMappingURL=useTokenizedInputInfo.js.map
