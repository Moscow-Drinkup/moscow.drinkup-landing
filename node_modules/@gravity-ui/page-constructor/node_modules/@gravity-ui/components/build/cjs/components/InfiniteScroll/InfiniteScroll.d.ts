import * as React from 'react';
import "./InfiniteScroll.css";
export interface InfiniteScrollProps {
    /** When called shows loader and wait till the promise is fulfilled to hide loader */
    onActivate: () => Promise<void>;
    /** Turn off activation */
    disabled: boolean;
    children: React.ReactNode;
    /** Custom loader */
    loader?: React.ReactNode;
}
export declare const InfiniteScroll: ({ onActivate, disabled, children, loader }: InfiniteScrollProps) => import("react/jsx-runtime").JSX.Element;
