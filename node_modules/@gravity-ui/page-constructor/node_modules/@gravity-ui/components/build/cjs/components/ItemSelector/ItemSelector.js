"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.ItemSelector = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
const React = __importStar(require("react"));
const icons_1 = require("@gravity-ui/icons");
const uikit_1 = require("@gravity-ui/uikit");
const cn_1 = require("../utils/cn.js");
const i18n_1 = require("./i18n/index.js");
require("./ItemSelector.css");
const b = (0, cn_1.block)('item-selector');
function getItemIdDefault(item) {
    return `${item}`;
}
class ItemSelector extends React.Component {
    static defaultProps = {
        hideSelected: true,
        selectorTitle: '',
        getItemId: getItemIdDefault,
    };
    renderItemTitle = (item) => {
        const { renderItemValue, getItemId } = this.props;
        if (renderItemValue) {
            return renderItemValue(item);
        }
        return getItemId(item);
    };
    renderItem = (item, active) => {
        const title = this.renderItemTitle(item);
        const nativeTitle = typeof title === 'string' ? title : undefined;
        return ((0, jsx_runtime_1.jsxs)("div", { className: b('item', { active }), children: [(0, jsx_runtime_1.jsx)("span", { className: b('item-text'), title: nativeTitle, children: title }), (0, jsx_runtime_1.jsx)(uikit_1.Button, { view: "flat-secondary", size: "s", className: b('item-select'), onClick: this.onAddItem.bind(this, item), children: (0, jsx_runtime_1.jsx)(i18n_1.i18n.Translation, { children: ({ t }) => t('button_select') }) })] }));
    };
    filterItem = (filter) => (item) => {
        const { getItemId } = this.props;
        return getItemId(item).includes(filter);
    };
    renderValueItem = (item, active) => {
        const title = this.renderItemTitle(item);
        const nativeTitle = typeof title === 'string' ? title : undefined;
        return ((0, jsx_runtime_1.jsxs)("div", { className: b('value-item', { active }), children: [(0, jsx_runtime_1.jsx)("span", { className: b('value-item-text'), title: nativeTitle, children: title }), (0, jsx_runtime_1.jsx)(uikit_1.Button, { view: "flat-secondary", size: "s", className: b('value-item-remove'), onClick: () => this.onRemoveItem(item), children: (0, jsx_runtime_1.jsx)(uikit_1.Icon, { data: icons_1.Xmark, size: 16 }) })] }));
    };
    getActualItems() {
        const { items, value, hideSelected, getItemId } = this.props;
        const actualItems = [];
        const selectedItems = new Array(value.length);
        const usedItems = new Map(value.map((id, index) => [id, index]));
        for (const item of items) {
            const selected = usedItems.get(getItemId(item));
            if (selected !== undefined) {
                selectedItems[selected] = item;
            }
            if (!hideSelected || selected === undefined) {
                actualItems.push(item);
            }
        }
        return [actualItems, selectedItems];
    }
    onAddItem = (item) => {
        const { getItemId, value } = this.props;
        const itemId = getItemId(item);
        const usedItems = new Set(value);
        const newValue = usedItems.has(itemId) ? value : [...value, itemId];
        setTimeout(() => {
            this.onUpdate(newValue);
        }, 0);
    };
    onRemoveItem = (item) => {
        const { value, getItemId } = this.props;
        const itemId = getItemId(item);
        const newValue = value.filter((id) => id !== itemId);
        setTimeout(() => {
            this.onUpdate(newValue);
        }, 0);
    };
    onErase = () => {
        this.onUpdate([]);
    };
    onSelectAll = () => {
        const { items, getItemId } = this.props;
        const value = items.map(getItemId);
        this.onUpdate(value);
    };
    onMoveItem = ({ oldIndex, newIndex }) => {
        if (oldIndex !== newIndex) {
            const value = this.props.value.slice();
            this.onUpdate(uikit_1.List.moveListElement(value, oldIndex, newIndex));
        }
    };
    onUpdate = (value) => {
        this.props.onUpdate(value);
    };
    render() {
        const { value, selectorTitle, renderItem = this.renderItem, renderValueItem = this.renderValueItem, filterItem = this.filterItem, hideSelectAllButton, } = this.props;
        const [items, selected] = this.getActualItems();
        return ((0, jsx_runtime_1.jsx)(i18n_1.i18n.Translation, { children: ({ t }) => ((0, jsx_runtime_1.jsxs)("div", { className: b(), children: [(0, jsx_runtime_1.jsxs)("div", { className: b('list'), children: [(0, jsx_runtime_1.jsxs)("div", { className: b('list-header'), children: [(0, jsx_runtime_1.jsx)("span", { className: b('list-title'), children: selectorTitle }), !hideSelectAllButton && ((0, jsx_runtime_1.jsx)(uikit_1.Button, { view: "flat", size: "s", disabled: items.length === 0, onClick: this.onSelectAll, children: t('button_select-all') }))] }), (0, jsx_runtime_1.jsx)(uikit_1.List, { items: items, renderItem: renderItem, filterItem: filterItem, filterPlaceholder: t('placeholder_search'), itemsHeight: 196 })] }), (0, jsx_runtime_1.jsxs)("div", { className: b('list'), children: [(0, jsx_runtime_1.jsxs)("div", { className: b('list-header'), children: [(0, jsx_runtime_1.jsx)("span", { className: b('list-title'), children: `${t('label_selected')}: ${value.length}` }), (0, jsx_runtime_1.jsx)(uikit_1.Button, { view: "flat", size: "s", disabled: value.length === 0, onClick: this.onErase, children: t('button_deselect-all') })] }), (0, jsx_runtime_1.jsx)(uikit_1.List, { items: selected, renderItem: renderValueItem, filterItem: filterItem, filterPlaceholder: t('placeholder_search'), sortable: true, onSortEnd: this.onMoveItem, itemsHeight: 196 })] })] })) }));
    }
}
exports.ItemSelector = ItemSelector;
//# sourceMappingURL=ItemSelector.js.map
