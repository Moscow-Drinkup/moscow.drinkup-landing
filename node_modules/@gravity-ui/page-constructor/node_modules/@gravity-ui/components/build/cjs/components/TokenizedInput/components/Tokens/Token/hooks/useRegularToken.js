"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.useRegularToken = void 0;
const React = __importStar(require("react"));
const constants_1 = require("../../../../constants.js");
const context_1 = require("../../../../context/index.js");
const hooks_1 = require("../../../../hooks/index.js");
const useTokenCallbacks_1 = require("./useTokenCallbacks.js");
const useRegularToken = (idx) => {
    const inputInfo = (0, context_1.useInputContext)();
    const { Field } = (0, context_1.useTokenizedInputComponents)();
    const { tokens, isEditable, fields, placeholder } = inputInfo.state;
    const { onApplyChanges, onRemoveToken } = inputInfo.callbacks;
    const { onChangeField, onFocusField } = (0, useTokenCallbacks_1.useTokenCallbacks)();
    const token = tokens[idx];
    const hasChanges = React.useRef(false);
    React.useEffect(() => {
        hasChanges.current = false;
    }, [idx]);
    const handleChangeField = React.useCallback((index, key, value) => {
        hasChanges.current = true;
        onChangeField(index, key, value);
    }, [onChangeField]);
    const blurCallback = React.useCallback(() => {
        if (hasChanges.current) {
            onApplyChanges(true);
            hasChanges.current = false;
        }
    }, [onApplyChanges]);
    const onBlur = (0, hooks_1.useApplyCallbackOnBlur)(blurCallback);
    const showRemoveButton = !token.options?.notRemovable;
    const onRemove = React.useCallback(() => {
        onRemoveToken(idx);
        onFocusField(idx, fields[0].key);
    }, [fields, idx, onFocusField, onRemoveToken]);
    const classNames = React.useMemo(() => ({
        wrapper: (0, constants_1.b)('token-wrapper', {
            error: Boolean(Object.keys(token.errors ?? {}).length),
        }),
        removeButton: (0, constants_1.b)('token-remove-button'),
    }), [token.errors]);
    const getPlaceholder = React.useCallback((i) => {
        if (typeof placeholder !== 'function') {
            return undefined;
        }
        return placeholder('regular', token.value, i);
    }, [placeholder, token]);
    return React.useMemo(() => ({
        state: {
            token,
            fields,
            showRemoveButton,
            Field,
            isEditable,
            classNames,
        },
        callbacks: {
            onChangeField: handleChangeField,
            onFocusField,
            onRemove,
            onBlur,
            getPlaceholder,
        },
    }), [
        Field,
        classNames,
        fields,
        handleChangeField,
        isEditable,
        onBlur,
        onFocusField,
        onRemove,
        showRemoveButton,
        token,
        getPlaceholder,
    ]);
};
exports.useRegularToken = useRegularToken;
//# sourceMappingURL=useRegularToken.js.map
