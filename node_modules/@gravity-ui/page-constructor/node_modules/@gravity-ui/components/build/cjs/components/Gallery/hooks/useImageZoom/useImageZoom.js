"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.useImageZoom = useImageZoom;
const React = __importStar(require("react"));
const uikit_1 = require("@gravity-ui/uikit");
const useLatest_1 = require("../useLatest.js");
const constants_1 = require("./constants.js");
const useImageZoomDesktop_1 = require("./useImageZoomDesktop.js");
const useImageZoomTouch_1 = require("./useImageZoomTouch.js");
const utils_1 = require("./utils.js");
/** Hook for managing image zoom and pan functionality in Gallery */
function useImageZoom({ disabled, onTap }) {
    const isMobile = (0, uikit_1.useMobile)();
    const [scale, setScale] = React.useState(1);
    const [position, setPosition] = React.useState({ x: 0, y: 0 });
    const [imageSize, setImageSize] = React.useState({ width: 0, height: 0 });
    const [containerSize, setContainerSize] = React.useState({ width: 0, height: 0 });
    const imageSizeRef = (0, useLatest_1.useLatest)(imageSize);
    const containerSizeRef = (0, useLatest_1.useLatest)(containerSize);
    const constrainPosition = React.useMemo(() => (0, utils_1.createConstrainPosition)(imageSizeRef, containerSizeRef), [imageSizeRef, containerSizeRef]);
    const imageFitsContainer = React.useMemo(() => (0, utils_1.checkImageFitsContainer)(imageSize, containerSize, scale), [imageSize, containerSize, scale]);
    const resetZoom = React.useCallback(() => {
        setScale(1);
        setPosition({ x: 0, y: 0 });
    }, []);
    const zoomState = { scale, position };
    const zoomActions = { setScale, setPosition, resetZoom };
    const constraints = {
        imageSize,
        containerSize,
        imageSizeRef,
        containerSizeRef,
        constrainPosition,
        imageFitsContainer,
    };
    const desktop = (0, useImageZoomDesktop_1.useImageZoomDesktop)({
        enabled: !isMobile && !disabled,
        zoomState,
        zoomActions,
        constraints,
    });
    const touch = (0, useImageZoomTouch_1.useImageZoomTouch)({
        enabled: isMobile && !disabled,
        zoomState,
        zoomActions,
        constraints,
        onTap,
    });
    React.useEffect(() => {
        if (scale > constants_1.MIN_SCALE) {
            const constrainedPosition = constrainPosition(position, scale);
            if (constrainedPosition.x !== position.x || constrainedPosition.y !== position.y) {
                setPosition(constrainedPosition);
            }
        }
    }, [imageSize, containerSize, scale, position, constrainPosition]);
    return {
        isZooming: desktop.isDragging || touch.isTouching || scale > constants_1.MIN_SCALE,
        imageHandlers: isMobile ? touch.handlers : desktop.handlers,
        setImageSize,
        setContainerSize,
        resetZoom,
        imageStyles: getImageStyles(),
    };
    function getImageStyles() {
        const isScaling = scale > constants_1.MIN_SCALE;
        const isInteracting = desktop.isDragging || touch.isTouching;
        const styles = {
            cursor: isScaling ? 'move' : 'zoom-in',
            transform: `translate(${position.x}px, ${position.y}px) scale(${scale})`,
            transition: isInteracting ? 'none' : 'transform 0.3s ease-out',
        };
        // Show zoom-out cursor when zoomed but image fits in container
        if (isScaling && imageFitsContainer)
            styles.cursor = 'zoom-out';
        return styles;
    }
}
//# sourceMappingURL=useImageZoom.js.map
