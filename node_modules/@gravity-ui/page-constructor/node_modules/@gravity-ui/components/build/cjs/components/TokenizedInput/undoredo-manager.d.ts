type UndoRedoState<T extends unknown> = {
    value: T;
    next: T[];
    prev: T[];
};
export declare class UndoRedoManager<T extends unknown> {
    _state: UndoRedoState<T> | undefined;
    constructor(value: T);
    private get state();
    init(value: T): void;
    update(value: T, force?: boolean): void;
    undo(): T;
    redo(): T;
    getValue(): T;
}
export {};
