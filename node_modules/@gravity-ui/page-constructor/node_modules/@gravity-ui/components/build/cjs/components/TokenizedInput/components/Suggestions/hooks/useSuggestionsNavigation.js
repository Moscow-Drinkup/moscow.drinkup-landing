"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.useSuggestionsNavigation = void 0;
const React = __importStar(require("react"));
const constants_1 = require("../../../constants.js");
const context_1 = require("../../../context/index.js");
const useSuggestionsNavigation = ({ inputElement, onKeyDown, onSelectNext, onSelectPrev, onApply, suggestion, currentWord, }) => {
    const focusInfo = (0, context_1.useFocusContext)();
    const { focus } = focusInfo.state;
    React.useEffect(() => {
        // We use a native DOM event listener here instead of React's synthetic onKeyDown
        // to ensure we intercept navigation keys (ArrowUp, ArrowDown, Enter) *before*
        // the main Wrapper's synthetic onKeyDown handler processes them.
        // This allows suggestions navigation to take precedence over token navigation
        // when the suggestions popup is open.
        const handleNavigation = (e) => {
            const preventOtherKeys = onKeyDown?.({
                event: e,
                suggestion,
                value: inputElement?.value || '',
                focus,
                onApply,
                onSelectNext,
                onSelectPrev,
                currentWord,
            }) ?? false;
            if (preventOtherKeys) {
                return;
            }
            const next = (e.ctrlKey && e.code === 'KeyN') || e.key === constants_1.KeyCode.ArrowDown;
            const prev = (e.ctrlKey && e.code === 'KeyP') || e.key === constants_1.KeyCode.ArrowUp;
            const select = suggestion && !e.metaKey && !e.ctrlKey && e.key === constants_1.KeyCode.Enter;
            switch (true) {
                case next: {
                    e.preventDefault();
                    e.stopPropagation();
                    onSelectNext();
                    break;
                }
                case prev: {
                    e.stopPropagation();
                    e.preventDefault();
                    onSelectPrev();
                    break;
                }
                case select: {
                    e.preventDefault();
                    e.stopPropagation();
                    onApply(suggestion);
                    break;
                }
                default: {
                    break;
                }
            }
        };
        inputElement?.addEventListener('keydown', handleNavigation);
        return () => {
            inputElement?.removeEventListener('keydown', handleNavigation);
        };
    }, [
        onSelectNext,
        onSelectPrev,
        onApply,
        suggestion,
        onKeyDown,
        currentWord,
        focus,
        inputElement,
    ]);
};
exports.useSuggestionsNavigation = useSuggestionsNavigation;
//# sourceMappingURL=useSuggestionsNavigation.js.map
