"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.useKeyDownHandler = void 0;
const React = __importStar(require("react"));
const context_1 = require("../../../context/index.js");
const useBlurHandler_1 = require("./useBlurHandler.js");
const useDeleteHandler_1 = require("./useDeleteHandler.js");
const useNavigationHandler_1 = require("./useNavigationHandler.js");
const useShortcuts_1 = require("./useShortcuts.js");
const useUndoRedoHandler_1 = require("./useUndoRedoHandler.js");
const useKeyDownHandler = () => {
    const shortcuts = (0, useShortcuts_1.useShortcuts)();
    const focusInfo = (0, context_1.useFocusContext)();
    const inputInfo = (0, context_1.useInputContext)();
    const options = (0, context_1.useOptionsContext)();
    const { fields, tokens } = inputInfo.state;
    const { onChangeToken, onApplyChanges } = inputInfo.callbacks;
    const { focus } = focusInfo.state;
    const { onFocus } = focusInfo.callbacks;
    const { onKeyDown } = options;
    const getCursorOffset = React.useCallback((input) => {
        if (!input.value || input.readOnly) {
            return undefined;
        }
        return input.selectionStart === input.value.length ? -1 : (input.selectionStart ?? 0);
    }, []);
    const reservedKeys = React.useMemo(() => {
        return fields.flatMap(({ key, specialKeysActions }) => specialKeysActions?.map((action) => ({
            ...action,
            fieldKey: key,
        })) ?? []);
    }, [fields]);
    const checkKey = React.useCallback((e, key) => {
        if (e.key === key &&
            !reservedKeys.some((reserved) => {
                let isReservedKey = false;
                if (typeof reserved.key === 'string') {
                    isReservedKey = reserved.key === e.key;
                }
                else {
                    isReservedKey = reserved.key(e);
                }
                return isReservedKey && focus?.key === reserved.fieldKey;
            })) {
            return true;
        }
        return false;
    }, [focus, reservedKeys]);
    const navigationHandler = (0, useNavigationHandler_1.useNavigationHandler)({
        shortcuts,
        checkKey,
        getCursorOffset,
    });
    const deleteHandler = (0, useDeleteHandler_1.useDeleteHandler)({
        shortcuts,
        checkKey,
    });
    const blurHandler = (0, useBlurHandler_1.useBlurHandler)({
        shortcuts,
        checkKey,
        getCursorOffset,
    });
    const undoRedo = (0, useUndoRedoHandler_1.useUndoRedoHandler)({
        shortcuts,
    });
    const specialKeysActionsHandler = React.useCallback((e) => {
        const input = e.target;
        if (!focus || !input) {
            return false;
        }
        const field = fields.find(({ key }) => key === focus?.key);
        const action = field?.specialKeysActions?.find(({ key }) => {
            if (typeof key === 'string') {
                return key === e.key;
            }
            return key(e);
        })?.action;
        if (!action) {
            return false;
        }
        const token = tokens[focus.idx] ?? {
            id: `tokenNew${tokens.length}`,
            kind: 'new',
            value: {},
        };
        action({
            token,
            offset: input.selectionStart ?? 0,
            focus,
            onFocus,
            onChange: onChangeToken,
            onApply: onApplyChanges,
            event: e,
        });
        return true;
    }, [fields, focus, onApplyChanges, onChangeToken, onFocus, tokens]);
    const externalKeyDown = React.useCallback((e) => {
        const input = e.target;
        if (!focus || !input) {
            return false;
        }
        const token = tokens[focus.idx] ?? {
            id: `tokenNew${tokens.length}`,
            kind: 'new',
            value: {},
        };
        return (onKeyDown?.({
            token,
            offset: input.selectionStart ?? 0,
            focus,
            onFocus,
            onChange: onChangeToken,
            onApply: onApplyChanges,
            event: e,
        }) ?? false);
    }, [focus, onApplyChanges, onChangeToken, onFocus, onKeyDown, tokens]);
    return React.useCallback((e) => {
        switch (true) {
            case externalKeyDown(e): {
                break;
            }
            case specialKeysActionsHandler(e): {
                break;
            }
            case navigationHandler(e): {
                break;
            }
            case deleteHandler(e): {
                break;
            }
            case blurHandler(e): {
                break;
            }
            case undoRedo(e): {
                break;
            }
        }
    }, [
        blurHandler,
        deleteHandler,
        externalKeyDown,
        navigationHandler,
        specialKeysActionsHandler,
        undoRedo,
    ]);
};
exports.useKeyDownHandler = useKeyDownHandler;
//# sourceMappingURL=useKeyDownHandler.js.map
