"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.ImageView = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
const React = __importStar(require("react"));
const uikit_1 = require("@gravity-ui/uikit");
const cn_1 = require("../../../../utils/cn.js");
const GalleryContext_1 = require("../../../contexts/GalleryContext/index.js");
const useImageZoom_1 = require("../../../hooks/useImageZoom/index.js");
const FallbackText_1 = require("../../FallbackText/index.js");
require("./ImageView.css");
const cnImageView = (0, cn_1.block)('gallery-image-view');
const ImageView = ({ className, src, alt = '' }) => {
    const [status, setStatus] = React.useState('loading');
    const imageRef = React.useRef(null);
    const containerRef = React.useRef(null);
    const isMobile = (0, uikit_1.useMobile)();
    const { onTap, onViewInteractionChange } = (0, GalleryContext_1.useGalleryContext)();
    const { imageHandlers, setImageSize, setContainerSize, resetZoom, imageStyles, isZooming } = (0, useImageZoom_1.useImageZoom)({ onTap });
    React.useEffect(() => {
        onViewInteractionChange(isZooming);
    }, [isZooming, onViewInteractionChange]);
    const handleLoad = React.useCallback(() => {
        setStatus('complete');
        if (imageRef.current) {
            setImageSize({
                width: imageRef.current.naturalWidth,
                height: imageRef.current.naturalHeight,
            });
        }
    }, [setImageSize]);
    const handleError = React.useCallback(() => {
        setStatus('error');
    }, []);
    // Track container dimensions and handle resize
    React.useEffect(() => {
        if (!containerRef.current)
            return undefined;
        const updateSize = () => {
            if (containerRef.current) {
                const size = {
                    width: containerRef.current.clientWidth,
                    height: containerRef.current.clientHeight,
                };
                // Only update if dimensions are valid
                if (size.width > 0 && size.height > 0) {
                    setContainerSize(size);
                }
            }
        };
        const resizeObserver = new ResizeObserver(() => {
            updateSize();
        });
        resizeObserver.observe(containerRef.current);
        updateSize();
        const timeoutId = setTimeout(updateSize, 100);
        window.addEventListener('resize', updateSize);
        return () => {
            resizeObserver.disconnect();
            clearTimeout(timeoutId);
            window.removeEventListener('resize', updateSize);
        };
    }, [setContainerSize]);
    React.useEffect(() => {
        resetZoom();
    }, [src, resetZoom]);
    if (status === 'error') {
        return (0, jsx_runtime_1.jsx)(FallbackText_1.GalleryFallbackText, {});
    }
    return ((0, jsx_runtime_1.jsxs)("div", { ref: containerRef, className: cnImageView({ mobile: isMobile }, className), children: [status === 'loading' && (0, jsx_runtime_1.jsx)(uikit_1.Spin, { className: cnImageView('spin'), size: "xl" }), (0, jsx_runtime_1.jsx)("img", { ref: imageRef, className: cnImageView('image'), src: src, alt: alt, ...imageHandlers, onLoad: handleLoad, onError: handleError, style: imageStyles })] }));
};
exports.ImageView = ImageView;
//# sourceMappingURL=ImageView.js.map
