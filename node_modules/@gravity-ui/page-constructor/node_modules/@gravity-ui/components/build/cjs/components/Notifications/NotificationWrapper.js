"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.NotificationWrapper = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
const React = __importStar(require("react"));
const uikit_1 = require("@gravity-ui/uikit");
const Notification_1 = require("../Notification/index.js");
const NotificationWithSwipe_1 = require("../Notification/NotificationWithSwipe.js");
const cn_1 = require("../utils/cn.js");
require("./Notifications.css");
const b = (0, cn_1.block)('notifications');
const NotificationWrapper = (props) => {
    const ref = React.useRef(null);
    const { notification, swipeThreshold } = props;
    const mobile = (0, uikit_1.useMobile)();
    const [isRemoved, setIsRemoved] = React.useState(false);
    React.useEffect(() => {
        const element = ref.current;
        if (!element) {
            if (!notification.archived && isRemoved) {
                setIsRemoved(false);
            }
            return () => { };
        }
        if (notification.archived) {
            const listener = (event) => {
                if (event.propertyName === 'max-height') {
                    setIsRemoved(true);
                    element.removeEventListener('transitionend', listener);
                }
            };
            element.addEventListener('transitionend', listener);
            element.style.maxHeight = `${element.scrollHeight}px`;
            element.style.transition = 'max-height 0.3s';
            // Firefox batches style changes made within a single frame, so setting maxHeight
            // to scrollHeight and then to 0px in the same frame skips the transition entirely.
            // Two nested requestAnimationFrame calls guarantee the browser commits the initial
            // maxHeight in one frame before applying 0px in the next, so the animation runs.
            requestAnimationFrame(() => {
                requestAnimationFrame(() => {
                    element.style.maxHeight = '0px';
                });
            });
            return () => {
                element.removeEventListener('transitionend', listener);
            };
        }
        setIsRemoved(false);
        return () => { };
    }, [notification.archived, isRemoved]);
    if (isRemoved) {
        return null;
    }
    return ((0, jsx_runtime_1.jsx)("li", { className: b('item'), children: (0, jsx_runtime_1.jsx)("div", { className: b('notification-wrapper', {
                archived: notification.archived,
                unread: notification.unread,
                active: Boolean(notification.onClick),
            }), ref: ref, children: mobile && notification.swipeActions ? ((0, jsx_runtime_1.jsx)(NotificationWithSwipe_1.NotificationWithSwipe, { notification: notification, swipeThreshold: swipeThreshold, wrapperRef: ref })) : ((0, jsx_runtime_1.jsx)(Notification_1.Notification, { notification: notification, wrapperRef: ref })) }) }, notification.id));
};
exports.NotificationWrapper = NotificationWrapper;
//# sourceMappingURL=NotificationWrapper.js.map
