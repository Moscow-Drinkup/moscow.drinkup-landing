"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.useSuggestionsInitialCall = void 0;
const React = __importStar(require("react"));
const useSuggestionsInitialCall = (focus, debounceFlushStrategy) => {
    const initialCallRef = React.useRef(true);
    React.useEffect(() => {
        if (debounceFlushStrategy === 'focus-input') {
            initialCallRef.current = !focus;
        }
        else {
            initialCallRef.current = true;
        }
        // We only want to reset the initial call flag when the focused field changes,
        // not when the entire focus object reference or debounceFlushStrategy changes.
        // eslint-disable-next-line react-hooks/exhaustive-deps
    }, [focus?.key, focus?.idx]);
    const setInitialCall = React.useCallback((value) => {
        initialCallRef.current = value;
    }, []);
    return React.useMemo(() => ({
        value: initialCallRef,
        setValue: setInitialCall,
    }), [setInitialCall]);
};
exports.useSuggestionsInitialCall = useSuggestionsInitialCall;
//# sourceMappingURL=useSuggestionsInitialCall.js.map
