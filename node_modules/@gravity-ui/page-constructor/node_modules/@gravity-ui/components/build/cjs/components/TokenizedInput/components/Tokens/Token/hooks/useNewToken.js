"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.useNewToken = void 0;
const React = __importStar(require("react"));
const constants_1 = require("../../../../constants.js");
const context_1 = require("../../../../context/index.js");
const utils_1 = require("../../../../utils.js");
const useTokenCallbacks_1 = require("./useTokenCallbacks.js");
const useNewToken = (idx) => {
    const inputInfo = (0, context_1.useInputContext)();
    const focusInfo = (0, context_1.useFocusContext)();
    const { Field } = (0, context_1.useTokenizedInputComponents)();
    const { tokens, fields, placeholder } = inputInfo.state;
    const { autoFocus } = focusInfo.state;
    const { onChangeField, onFocusField } = (0, useTokenCallbacks_1.useTokenCallbacks)();
    const token = React.useMemo(() => tokens[idx] ?? { id: 'new-token', value: (0, utils_1.getDefaultTokenValue)(fields), kind: 'new' }, [fields, idx, tokens]);
    const checkIsHidden = React.useCallback((i) => i > 0 && !token.value[fields[i - 1].key], [fields, token.value]);
    const checkIsAutoFocus = React.useCallback((i) => i === 0 && autoFocus, [autoFocus]);
    const getPlaceholder = React.useCallback((i) => {
        if (typeof placeholder === 'function') {
            return placeholder('new', token.value, i);
        }
        return i === 0 ? placeholder : undefined;
    }, [placeholder, token]);
    const classNames = React.useMemo(() => ({
        wrapper: (0, constants_1.b)('token-wrapper', { new: true }),
    }), []);
    return React.useMemo(() => ({
        state: {
            token,
            fields,
            Field,
            classNames,
        },
        callbacks: {
            onChangeField,
            onFocusField,
            getPlaceholder,
            checkIsHidden,
            checkIsAutoFocus,
        },
    }), [
        Field,
        checkIsAutoFocus,
        checkIsHidden,
        classNames,
        fields,
        getPlaceholder,
        onChangeField,
        onFocusField,
        token,
    ]);
};
exports.useNewToken = useNewToken;
//# sourceMappingURL=useNewToken.js.map
