"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Reactions = Reactions;
const jsx_runtime_1 = require("react/jsx-runtime");
const React = __importStar(require("react"));
const icons_1 = require("@gravity-ui/icons");
const uikit_1 = require("@gravity-ui/uikit");
const xor_1 = __importDefault(require("lodash/xor.js"));
const cn_1 = require("../utils/cn.js");
const Reaction_1 = require("./Reaction.js");
const ReactionsContainer_1 = require("./ReactionsContainer.js");
const context_1 = require("./context.js");
const i18n_1 = require("./i18n/index.js");
require("./Reactions.css");
const b = (0, cn_1.block)('reactions');
const buttonSizeToIconSize = {
    xs: '12px',
    s: '16px',
    m: '16px',
    l: '16px',
    xl: '20px',
};
const popupId = 'reactions-palette-popup';
function Reactions({ reactions, reactionsState, className, style, size = 'm', paletteProps, readOnly, qa, addButtonPlacement = 'end', renderTooltip, onToggle, popupClassName, popupPlacement, renderReaction, renderAddReaction, renderReactionsContent, }) {
    const { t } = i18n_1.i18n.useTranslation();
    const addReactionButtonRef = React.useRef(null);
    const addReactionRef = React.useRef(null);
    const [palettePopupOpened, setPalettePopupOpened] = React.useState(false);
    const onOpenPalettePopup = React.useCallback(() => setPalettePopupOpened(true), []);
    const onClosePalettePopup = React.useCallback(() => setPalettePopupOpened(false), []);
    const onTogglePalettePopup = React.useCallback(() => (palettePopupOpened ? onClosePalettePopup() : onOpenPalettePopup()), [onClosePalettePopup, onOpenPalettePopup, palettePopupOpened]);
    const [currentHoveredReaction, setCurrentHoveredReaction] = React.useState(undefined);
    const paletteOptionsMap = React.useMemo(() => reactions.reduce((acc, current) => {
        // eslint-disable-next-line no-param-reassign
        acc[current.value] = current;
        return acc;
    }, {}), [reactions]);
    const paletteValue = React.useMemo(() => reactionsState
        .filter((reaction) => reaction.selected)
        .map((reaction) => reaction.value), [reactionsState]);
    const onUpdatePalette = React.useCallback((updated) => {
        const diffValues = (0, xor_1.default)(paletteValue, updated);
        for (const diffValue of diffValues) {
            onToggle?.(diffValue);
        }
        onClosePalettePopup();
    }, [onClosePalettePopup, onToggle, paletteValue]);
    const paletteContent = React.useMemo(() => ((0, jsx_runtime_1.jsx)(uikit_1.Palette, { size: size, options: reactions, value: paletteValue, onUpdate: onUpdatePalette, ...paletteProps })), [paletteProps, reactions, paletteValue, size, onUpdatePalette]);
    const addReactionButton = React.useMemo(() => {
        if (readOnly) {
            return null;
        }
        if (renderAddReaction) {
            return renderAddReaction({
                paletteOpened: palettePopupOpened,
                ref: addReactionRef,
                triggerProps: {
                    onClick: onTogglePalettePopup,
                    'aria-expanded': palettePopupOpened,
                    'aria-haspopup': 'true',
                    'aria-controls': popupId,
                },
            });
        }
        return ((0, jsx_runtime_1.jsx)(uikit_1.Button, { ref: addReactionButtonRef, size: size, "aria-label": t('add-reaction'), "aria-expanded": palettePopupOpened, "aria-haspopup": true, "aria-controls": popupId, onClick: onTogglePalettePopup, view: "flat-secondary", className: b('reaction-button', {
                active: palettePopupOpened,
            }), children: (0, jsx_runtime_1.jsx)(uikit_1.Button.Icon, { children: (0, jsx_runtime_1.jsx)(uikit_1.Icon, { data: icons_1.FaceSmile, size: buttonSizeToIconSize[size] }) }) }));
    }, [readOnly, renderAddReaction, size, onTogglePalettePopup, palettePopupOpened, t]);
    const addReactionPopup = readOnly ? null : ((0, jsx_runtime_1.jsx)(uikit_1.Popup, { id: popupId, anchorRef: renderAddReaction ? addReactionRef : addReactionButtonRef, className: b('add-reaction-popover', popupClassName), open: palettePopupOpened, modal: true, placement: popupPlacement, initialFocus: 0, onOutsideClick: onClosePalettePopup, onEscapeKeyDown: onClosePalettePopup, children: paletteContent }));
    const reactionList = ((0, jsx_runtime_1.jsx)(React.Fragment, { children: reactionsState.map((reaction) => {
            const content = paletteOptionsMap[reaction.value]?.content ?? '?';
            return ((0, jsx_runtime_1.jsx)(Reaction_1.Reaction, { content: content, reaction: reaction, size: size, tooltip: renderTooltip ? renderTooltip(reaction) : undefined, onClick: readOnly ? undefined : onToggle, renderReaction: renderReaction }, reaction.value));
        }) }));
    const renderReactions = () => {
        if (renderReactionsContent) {
            return renderReactionsContent({
                reactionList,
                addReactionButton,
            });
        }
        return ((0, jsx_runtime_1.jsxs)(ReactionsContainer_1.ReactionsContainer, { className: className, qa: qa, style: style, children: [addButtonPlacement === 'start' ? addReactionButton : null, reactionList, addButtonPlacement === 'end' ? addReactionButton : null] }));
    };
    return ((0, jsx_runtime_1.jsxs)(context_1.ReactionsContextProvider, { value: {
            openedTooltip: currentHoveredReaction,
            setOpenedTooltip: setCurrentHoveredReaction,
        }, children: [renderReactions(), addReactionPopup] }));
}
//# sourceMappingURL=Reactions.js.map
