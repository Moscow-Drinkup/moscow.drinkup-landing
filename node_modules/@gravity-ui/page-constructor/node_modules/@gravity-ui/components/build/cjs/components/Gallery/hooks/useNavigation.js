"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.useNavigation = useNavigation;
const React = __importStar(require("react"));
const uikit_1 = require("@gravity-ui/uikit");
const useLatest_1 = require("./useLatest.js");
function useNavigation({ initialItemIndex = 0, activeItemIndex: controlledActiveItemIndex, onActiveItemIndexChange, itemRefs, keyboardScope, }) {
    const itemsCount = itemRefs.length;
    const onActiveItemIndexChangeRef = (0, useLatest_1.useLatest)(onActiveItemIndexChange);
    const handleActiveItemIndexChange = React.useCallback((index) => {
        onActiveItemIndexChangeRef.current?.(index);
    }, [onActiveItemIndexChangeRef]);
    const [rawItemIndex, setRawItemIndex] = (0, uikit_1.useControlledState)(controlledActiveItemIndex, initialItemIndex, handleActiveItemIndexChange);
    const activeItemIndex = itemsCount === 0 ? 0 : Math.min(Math.max(rawItemIndex, 0), itemsCount - 1);
    const activeItemIndexRef = (0, useLatest_1.useLatest)(activeItemIndex);
    const setRawItemIndexRef = (0, useLatest_1.useLatest)(setRawItemIndex);
    const setActiveItemIndex = React.useCallback((update) => {
        const nextActiveItemIndex = typeof update === 'function' ? update(activeItemIndexRef.current) : update;
        setRawItemIndexRef.current(nextActiveItemIndex);
    }, [activeItemIndexRef, setRawItemIndexRef]);
    const handleGoToPrevious = React.useCallback(() => {
        setActiveItemIndex((previousActiveItemIndex) => {
            const nextActiveItemIndex = previousActiveItemIndex - 1;
            return nextActiveItemIndex > -1 ? nextActiveItemIndex : itemsCount - 1;
        });
    }, [itemsCount, setActiveItemIndex]);
    const handleGoToNext = React.useCallback(() => {
        setActiveItemIndex((previousActiveItemIndex) => (previousActiveItemIndex + 1) % itemsCount);
    }, [itemsCount, setActiveItemIndex]);
    React.useEffect(() => {
        const activeItemPreview = itemRefs[activeItemIndex]?.current;
        if (activeItemPreview) {
            activeItemPreview.scrollIntoView();
        }
    }, [activeItemIndex, itemRefs]);
    React.useEffect(() => {
        const target = keyboardScope?.current ?? document;
        const handleKeyDown = (event) => {
            const { key } = event;
            if (key === 'ArrowLeft') {
                handleGoToPrevious();
            }
            else if (key === 'ArrowRight') {
                handleGoToNext();
            }
        };
        target.addEventListener('keydown', handleKeyDown);
        return () => {
            target.removeEventListener('keydown', handleKeyDown);
        };
    }, [handleGoToNext, handleGoToPrevious, keyboardScope]);
    return {
        activeItemIndex,
        handleGoToPrevious,
        handleGoToNext,
        setActiveItemIndex,
    };
}
//# sourceMappingURL=useNavigation.js.map
