"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.swipeWithSwithingAnimation = exports.isTouchOnGalleryContent = void 0;
const constants_1 = require("../../constants.js");
const constants_2 = require("./constants.js");
// Helper function to check if touch target is on gallery content (not on overlay elements)
const isTouchOnGalleryContent = (target) => {
    if (!target || !(target instanceof Element)) {
        return false;
    }
    const element = target;
    // Check if the touch is within the gallery body content area
    const isInGalleryBody = element.closest(`.${(0, constants_1.cnGallery)(constants_1.BODY_CONTENT_CLASS_NAME)}`);
    return Boolean(isInGalleryBody);
};
exports.isTouchOnGalleryContent = isTouchOnGalleryContent;
const swipeWithSwithingAnimation = ({ swipeAction, isSwitching, setIsSwitching, }) => {
    if (isSwitching) {
        swipeAction();
    }
    else {
        setIsSwitching(true);
        setTimeout(() => {
            swipeAction();
            setTimeout(() => setIsSwitching(false), constants_2.SWITCHING_TIMEOUT);
        }, constants_2.SWIPE_TIMEOUT);
    }
};
exports.swipeWithSwithingAnimation = swipeWithSwithingAnimation;
//# sourceMappingURL=utils.js.map
