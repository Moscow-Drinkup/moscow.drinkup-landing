"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.useWrapper = void 0;
const React = __importStar(require("react"));
const constants_1 = require("../../../constants.js");
const context_1 = require("../../../context/index.js");
const hooks_1 = require("../../../hooks/index.js");
const useKeyDownHandler_1 = require("./useKeyDownHandler.js");
const useWrapper = () => {
    const focusInfo = (0, context_1.useFocusContext)();
    const inputInfo = (0, context_1.useInputContext)();
    const { tokens, fields, isEditable, isClearable, className, size, wrapperRef } = inputInfo.state;
    const { onApplyChanges, onClearInput } = inputInfo.callbacks;
    const { focus } = focusInfo.state;
    const { onBlur, onFocus } = focusInfo.callbacks;
    const blurCallback = React.useCallback(() => {
        onBlur();
        onApplyChanges();
    }, [onApplyChanges, onBlur]);
    const handleBlur = (0, hooks_1.useApplyCallbackOnBlur)(blurCallback);
    const handleKeyDown = (0, useKeyDownHandler_1.useKeyDownHandler)();
    const handleClear = React.useCallback(() => {
        onClearInput();
        onFocus({
            idx: tokens.length,
            key: fields[0].key,
        });
    }, [fields, onClearInput, onFocus, tokens.length]);
    const classNames = React.useMemo(() => ({
        wrapper: (0, constants_1.b)('wrapper', {
            disabled: !isEditable,
            focused: Boolean(focus),
            size,
        }, className),
        clearButton: (0, constants_1.b)('clear-button'),
    }), [className, focus, isEditable, size]);
    return React.useMemo(() => ({
        state: { isEditable, isClearable, classNames, wrapperRef },
        callbacks: {
            onBlur: handleBlur,
            onKeyDown: handleKeyDown,
            onClear: handleClear,
        },
    }), [classNames, handleBlur, handleClear, handleKeyDown, isClearable, isEditable, wrapperRef]);
};
exports.useWrapper = useWrapper;
//# sourceMappingURL=useWrapper.js.map
