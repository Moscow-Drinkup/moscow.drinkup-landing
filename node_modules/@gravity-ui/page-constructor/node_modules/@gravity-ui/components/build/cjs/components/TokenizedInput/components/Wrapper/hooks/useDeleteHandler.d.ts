import * as React from 'react';
import type { TokenValueBase } from "../../../types.js";
import type { ShortcutMap } from "./useShortcuts.js";
type UseDeleteHandlerOptions = {
    shortcuts: ShortcutMap;
    checkKey: (e: React.KeyboardEvent, key: string) => boolean;
};
export declare const useDeleteHandler: <T extends TokenValueBase>({ shortcuts, checkKey, }: UseDeleteHandlerOptions) => (e: React.KeyboardEvent) => boolean;
export {};
