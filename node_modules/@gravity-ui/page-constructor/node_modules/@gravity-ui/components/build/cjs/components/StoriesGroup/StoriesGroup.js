"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.StoriesGroup = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
const React = __importStar(require("react"));
const uikit_1 = require("@gravity-ui/uikit");
const StoriesLayout_1 = require("../Stories/components/StoriesLayout/StoriesLayout.js");
const cn_1 = require("../utils/cn.js");
const components_1 = require("./components/index.js");
require("./StoriesGroup.css");
const DEFAULT_MAX_SLIDER_ITEMS_COUNT = 12;
const b = (0, cn_1.block)('stories-group');
// eslint-disable-next-line complexity
const StoriesGroup = ({ open, groups, onItemSelect, disableOutsideClick = true, initialStoryIndex = [0, 0], maxSliderItemsCount = DEFAULT_MAX_SLIDER_ITEMS_COUNT, onClose, }) => {
    const [[groupIndex, itemIndex], setStoryIndex] = React.useState(initialStoryIndex);
    const handleClose = React.useCallback((event, reason) => {
        onClose?.(event, reason);
    }, [onClose]);
    const handleButtonClose = React.useCallback((event) => {
        handleClose(event, 'closeButtonClick');
    }, [handleClose]);
    const handleGotoPrevious = React.useCallback(() => {
        setStoryIndex((prevState) => {
            const [currentGroupIndex, currentItemIndex] = prevState;
            if (currentItemIndex > 0) {
                const newState = [currentGroupIndex, currentItemIndex - 1];
                onItemSelect?.(newState, false);
                return newState;
            }
            // try to find previous valid group
            for (let i = currentGroupIndex - 1; i >= 0; --i) {
                if (groups[i].items.length !== 0) {
                    const newState = [i, groups[i].items.length - 1];
                    onItemSelect?.(newState, false);
                    return newState;
                }
            }
            return prevState;
        });
    }, [groups, onItemSelect]);
    const handleGotoNext = React.useCallback(() => {
        setStoryIndex((prevState) => {
            const [currentGroupIndex, currentItemIndex] = prevState;
            if (currentItemIndex < groups[currentGroupIndex]?.items.length - 1) {
                const newState = [currentGroupIndex, currentItemIndex + 1];
                onItemSelect?.(newState, false);
                return newState;
            }
            // try to find next valid group
            for (let i = currentGroupIndex + 1; i < groups.length; ++i) {
                if (groups[i].items.length !== 0) {
                    const newState = [i, 0];
                    onItemSelect?.(newState, false);
                    return newState;
                }
            }
            return prevState;
        });
    }, [groups, onItemSelect]);
    const onGroupSelect = React.useCallback((newGroupIndex) => {
        setStoryIndex([newGroupIndex, 0]);
        onItemSelect?.([newGroupIndex, 0], true);
    }, [onItemSelect]);
    if (groups.length === 0) {
        return null;
    }
    const currentGroup = groups[groupIndex];
    const currentItems = currentGroup?.items || [];
    // case when groups has changed and indexs has ceased to be valid
    if (currentGroup === undefined || currentItems[itemIndex] === undefined) {
        if (groups[initialStoryIndex[0]] &&
            groups[initialStoryIndex[0]].items[initialStoryIndex[1]]) {
            setStoryIndex(initialStoryIndex);
        }
        else {
            // try to find first valid index
            for (let i = 0; i < groups.length; ++i) {
                if (groups[i] && groups[i].items.length !== 0) {
                    setStoryIndex([i, 0]);
                    break;
                }
            }
        }
        return null;
    }
    const indexType = (groups.length === 1 && currentItems.length === 1 && StoriesLayout_1.IndexType.Single) ||
        (groupIndex === 0 && itemIndex === 0 && StoriesLayout_1.IndexType.Start) ||
        (groupIndex === groups.length - 1 &&
            itemIndex === currentItems.length - 1 &&
            StoriesLayout_1.IndexType.End) ||
        StoriesLayout_1.IndexType.InProccess;
    return ((0, jsx_runtime_1.jsxs)(uikit_1.Modal, { open: open, onClose: handleClose, disableOutsideClick: disableOutsideClick, className: b(), contentClassName: b('modal-content'), children: [(0, jsx_runtime_1.jsx)(components_1.StoriesPreview, { maxSliderItemsCount: maxSliderItemsCount > 0 ? maxSliderItemsCount : 1, groups: groups, groupIndex: groupIndex, onGroupSelect: onGroupSelect, onClose: disableOutsideClick ? undefined : handleClose }), (0, jsx_runtime_1.jsx)(StoriesLayout_1.StoriesLayout, { storyIndex: itemIndex, items: currentItems, indexType: indexType, handleButtonClose: handleButtonClose, handleGotoNext: handleGotoNext, handleGotoPrevious: handleGotoPrevious })] }));
};
exports.StoriesGroup = StoriesGroup;
//# sourceMappingURL=StoriesGroup.js.map
