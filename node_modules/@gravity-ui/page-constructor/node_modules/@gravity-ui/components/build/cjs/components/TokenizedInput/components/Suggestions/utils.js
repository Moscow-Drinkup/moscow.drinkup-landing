"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.simpleFilterSuggestions = exports.sortSuggestions = void 0;
const sortSuggestions = (items) => {
    const itemsWithSort = items.filter((item) => item.sort !== undefined);
    const itemsWithoutSort = items.filter((item) => item.sort === undefined);
    return [
        ...itemsWithSort.sort((a, b) => (a.sort ?? 0) - (b.sort ?? 0)),
        ...itemsWithoutSort,
    ];
};
exports.sortSuggestions = sortSuggestions;
const simpleFilterSuggestions = (items, search) => {
    if (!search) {
        return items;
    }
    const searchLower = search.toLowerCase();
    const filteredItems = items
        .map((item) => ({
        ...item,
        search: item.search.toLowerCase(),
    }))
        .filter((item) => item.search.includes(searchLower))
        .sort((a, b) => {
        const startA = a.search.indexOf(searchLower);
        const startB = b.search.indexOf(searchLower);
        if (startA === startB) {
            return a.search.localeCompare(b.search);
        }
        return startA - startB;
    });
    return (0, exports.sortSuggestions)(filteredItems);
};
exports.simpleFilterSuggestions = simpleFilterSuggestions;
//# sourceMappingURL=utils.js.map
