import * as React from 'react';
import type { StoriesItem } from "../../types.js";
import "./StoriesLayout.css";
export declare enum IndexType {
    Start = 1,
    End = 2,
    InProccess = 3,
    Single = 4
}
export type StoriesLayoutProps = {
    items: StoriesItem[];
    storyIndex: number;
    indexType: IndexType;
    handleButtonClose: (event: MouseEvent | KeyboardEvent | React.MouseEvent<HTMLElement, MouseEvent>) => void;
    handleGotoPrevious: () => void;
    handleGotoNext: () => void;
};
export declare const StoriesLayout: ({ items, indexType, storyIndex, handleButtonClose, handleGotoNext, handleGotoPrevious, }: StoriesLayoutProps) => import("react/jsx-runtime").JSX.Element;
