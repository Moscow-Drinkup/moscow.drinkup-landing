import type { TokenValueBase, TokenizedSuggestions, TokenizedSuggestionsItem } from "../../../types.js";
import { SuggestionsNavigationOptions } from "../types.js";
type UseSelectSuggestionOptions<T extends TokenValueBase> = {
    suggestions: TokenizedSuggestions<T>;
    inputElement: HTMLInputElement | null;
    onApplySuggestion: (suggestion: TokenizedSuggestionsItem<T>) => void;
    onKeyDown?: (v: SuggestionsNavigationOptions<T>) => boolean;
};
export declare const useSelectSuggestion: <T extends TokenValueBase>({ suggestions, inputElement, onApplySuggestion, onKeyDown, }: UseSelectSuggestionOptions<T>) => {
    selected: number;
    handleSelectNext: () => void;
    handleSelectPrev: () => void;
};
export {};
