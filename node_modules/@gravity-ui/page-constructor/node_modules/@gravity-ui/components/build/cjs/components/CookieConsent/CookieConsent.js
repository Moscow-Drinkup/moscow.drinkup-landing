"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.CookieConsent = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
const React = __importStar(require("react"));
const cn_1 = require("../utils/cn.js");
const ConsentNotification_1 = require("./components/ConsentNotification/ConsentNotification.js");
const ConsentPopup_1 = require("./components/ConsentPopup/ConsentPopup.js");
const types_1 = require("./components/ConsentPopup/types.js");
const SimpleConsent_1 = require("./components/SimpleConsent/SimpleConsent.js");
const b = (0, cn_1.block)('analytics');
const CookieConsent = ({ consentManager, onConsentPopupClose, manageCookies, ...popupProps }) => {
    const [isOpened, setIsOpened] = React.useState(false);
    React.useEffect(() => {
        // Show banner after some timeout so that the user has time to see the service content
        const timeoutId = setTimeout(() => {
            if (consentManager.isConsentNotDefined()) {
                setIsOpened(true);
            }
        }, 1000);
        return () => clearTimeout(timeoutId);
    }, [consentManager]);
    const onConsentPopupAction = (values) => {
        consentManager.setConsents(values);
        setIsOpened(false);
        onConsentPopupClose?.();
    };
    const onClose = () => {
        setIsOpened(false);
        onConsentPopupClose?.();
    };
    const view = manageCookies ? 'manage' : consentManager.mode;
    if (isOpened || manageCookies) {
        switch (view) {
            case 'manage':
                return ((0, jsx_runtime_1.jsx)(ConsentPopup_1.ConsentPopup, { ...popupProps, className: b(), step: manageCookies ? types_1.ConsentPopupStep.Manage : types_1.ConsentPopupStep.Main, onAction: onConsentPopupAction, onClose: onClose, consentManager: consentManager }));
            case 'notification':
                return ((0, jsx_runtime_1.jsx)(ConsentNotification_1.ConsentNotification, { ...popupProps, className: b(), onAction: onConsentPopupAction, consentManager: consentManager }));
            case 'base':
                return ((0, jsx_runtime_1.jsx)(SimpleConsent_1.SimpleConsent, { ...popupProps, className: b(), onAction: onConsentPopupAction, consentManager: consentManager }));
        }
    }
    return null;
};
exports.CookieConsent = CookieConsent;
//# sourceMappingURL=CookieConsent.js.map
