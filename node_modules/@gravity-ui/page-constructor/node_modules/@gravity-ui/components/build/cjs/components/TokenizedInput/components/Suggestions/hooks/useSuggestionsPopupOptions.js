"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.useSuggestionsPopupOptions = void 0;
const React = __importStar(require("react"));
const context_1 = require("../../../context/index.js");
const useSuggestionsPopupOptions = (inputElement) => {
    const inputInfo = (0, context_1.useInputContext)();
    const options = (0, context_1.useOptionsContext)();
    const { wrapperRef } = inputInfo.state;
    const { fullWidthSuggestions } = options;
    const [popupWidth, setPopupWidth] = React.useState(fullWidthSuggestions ? wrapperRef.current?.offsetWidth : undefined);
    React.useEffect(() => {
        if (!fullWidthSuggestions || !wrapperRef.current) {
            return () => { };
        }
        const resizeObserver = new ResizeObserver((resizes) => {
            setPopupWidth(resizes[0].borderBoxSize[0].inlineSize);
        });
        resizeObserver.observe(wrapperRef.current);
        return () => {
            resizeObserver.disconnect();
        };
    }, [wrapperRef, fullWidthSuggestions]);
    const popupOffset = React.useMemo(() => {
        if (!fullWidthSuggestions) {
            return 0;
        }
        const inputX = inputElement?.getBoundingClientRect()?.x ?? 0;
        const wrapperX = wrapperRef.current?.getBoundingClientRect()?.x ?? 0;
        return { mainAxis: 0, crossAxis: wrapperX - inputX };
    }, [fullWidthSuggestions, inputElement, wrapperRef]);
    return { popupWidth, popupOffset };
};
exports.useSuggestionsPopupOptions = useSuggestionsPopupOptions;
//# sourceMappingURL=useSuggestionsPopupOptions.js.map
