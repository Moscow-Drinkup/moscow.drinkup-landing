"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.useBlurHandler = void 0;
const React = __importStar(require("react"));
const constants_1 = require("../../../constants.js");
const context_1 = require("../../../context/index.js");
const useBlurHandler = ({ shortcuts, checkKey, getCursorOffset, }) => {
    const focusInfo = (0, context_1.useFocusContext)();
    const inputInfo = (0, context_1.useInputContext)();
    const { focus } = focusInfo.state;
    const { getFocusRules, onFocus, onBlur } = focusInfo.callbacks;
    const { onApplyChanges } = inputInfo.callbacks;
    return React.useCallback((e) => {
        const handler = () => {
            const input = e.target;
            input.blur();
            onBlur();
        };
        if (checkKey(e, constants_1.KeyCode.Enter)) {
            e.preventDefault();
            onApplyChanges();
            if (shortcuts.isApplyModifier(e)) {
                handler();
                return true;
            }
            else {
                if (!focus) {
                    return false;
                }
                const input = e.target;
                const focusRules = getFocusRules({
                    ...focus,
                    offset: getCursorOffset(input),
                });
                onFocus(focusRules.nextToken);
                return true;
            }
        }
        if (checkKey(e, constants_1.KeyCode.Escape)) {
            e.preventDefault();
            handler();
            return true;
        }
        return false;
    }, [
        checkKey,
        focus,
        getCursorOffset,
        getFocusRules,
        onApplyChanges,
        onBlur,
        onFocus,
        shortcuts,
    ]);
};
exports.useBlurHandler = useBlurHandler;
//# sourceMappingURL=useBlurHandler.js.map
