import * as React from 'react';
import type { GalleryItemAction } from "../../GalleryItem.js";
export type GalleryHeaderProps = {
    itemName?: React.ReactNode;
    actions?: GalleryItemAction[];
    withNavigation: boolean;
    activeItemIndex: number;
    itemsLength: number;
    fullScreen: boolean;
    onGoToPrevious: () => void;
    onGoToNext: () => void;
    hidden?: boolean;
    interactive?: boolean;
    onClose?: () => void;
    onUpdateFullScreen?: React.Dispatch<React.SetStateAction<boolean>>;
};
export declare const GalleryHeader: ({ itemName, actions, withNavigation, activeItemIndex, itemsLength, fullScreen, onGoToPrevious, onGoToNext, onUpdateFullScreen, onClose, hidden, interactive, }: GalleryHeaderProps) => import("react/jsx-runtime").JSX.Element;
