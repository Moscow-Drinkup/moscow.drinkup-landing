"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.useNavigationHandler = void 0;
const React = __importStar(require("react"));
const constants_1 = require("../../../constants.js");
const context_1 = require("../../../context/index.js");
const useNavigationHandler = ({ shortcuts, checkKey, getCursorOffset, }) => {
    const focusInfo = (0, context_1.useFocusContext)();
    const inputInfo = (0, context_1.useInputContext)();
    const { focus } = focusInfo.state;
    const { getFocusRules, onFocus } = focusInfo.callbacks;
    const { fields, tokens } = inputInfo.state;
    // move to next field at end of word
    // move to previous field at start of word
    const moveToNeighborField = React.useCallback((e) => {
        if (!focus) {
            return false;
        }
        const input = e.target;
        const focusRules = getFocusRules({
            ...focus,
            offset: undefined,
        });
        const token = tokens[focus.idx];
        const isReadOnlyField = token?.options?.readOnlyFields?.includes(focus.key);
        if (token &&
            (input.selectionStart === input.value.length || isReadOnlyField) &&
            checkKey(e, constants_1.KeyCode.ArrowRight)) {
            e.preventDefault();
            onFocus({
                ...focusRules.nextField,
                offset: 0,
            });
            return true;
        }
        if ((focusRules.prevField.key !== focus.key ||
            focusRules.prevField.idx !== focus.idx) &&
            (input.selectionStart === 0 || isReadOnlyField) &&
            checkKey(e, constants_1.KeyCode.ArrowLeft)) {
            e.preventDefault();
            onFocus({
                ...focusRules.prevField,
                offset: -1,
            });
            return true;
        }
        return false;
    }, [checkKey, focus, getFocusRules, onFocus, tokens]);
    const tabJumping = React.useCallback((e) => {
        if (!focus) {
            return false;
        }
        const focusRules = getFocusRules({
            ...focus,
            offset: undefined,
        });
        if (checkKey(e, constants_1.KeyCode.Tab)) {
            if (e.shiftKey &&
                ((focus.idx === 0 && fields[0].key !== focus.key) || focus.idx > 0)) {
                e.preventDefault();
                onFocus(focusRules.prevField);
                return true;
            }
            if (!e.shiftKey && tokens[focus.idx]?.value) {
                e.preventDefault();
                onFocus(focusRules.nextField);
                return true;
            }
        }
        return false;
    }, [checkKey, fields, focus, getFocusRules, onFocus, tokens]);
    // move to next/previous field
    const jumpToNeighborField = React.useCallback((e) => {
        if (!focus) {
            return false;
        }
        const input = e.target;
        const focusRules = getFocusRules({
            ...focus,
            offset: getCursorOffset(input),
        });
        if (checkKey(e, constants_1.KeyCode.ArrowRight)) {
            e.preventDefault();
            onFocus(focusRules.nextField);
            return true;
        }
        if (checkKey(e, constants_1.KeyCode.ArrowLeft)) {
            e.preventDefault();
            onFocus(focusRules.prevField);
            return true;
        }
        return false;
    }, [checkKey, focus, getCursorOffset, getFocusRules, onFocus]);
    // move to next/previous token
    const jumpToNeighborToken = React.useCallback((e) => {
        if (!focus) {
            return false;
        }
        const input = e.target;
        const focusRules = getFocusRules({
            ...focus,
            offset: getCursorOffset(input),
        });
        if (checkKey(e, constants_1.KeyCode.ArrowRight)) {
            e.preventDefault();
            onFocus(focusRules.nextToken);
            return true;
        }
        if (checkKey(e, constants_1.KeyCode.ArrowLeft)) {
            e.preventDefault();
            onFocus(focusRules.prevToken);
            return true;
        }
        return false;
    }, [checkKey, focus, getCursorOffset, getFocusRules, onFocus]);
    return React.useCallback((e) => {
        if (!e.shiftKey) {
            if (shortcuts.isTokenModifier(e)) {
                return jumpToNeighborToken(e);
            }
            if (shortcuts.isFieldModifier(e)) {
                return jumpToNeighborField(e);
            }
            if (!checkKey(e, constants_1.KeyCode.Tab)) {
                return moveToNeighborField(e);
            }
        }
        return tabJumping(e);
    }, [
        checkKey,
        jumpToNeighborField,
        jumpToNeighborToken,
        moveToNeighborField,
        shortcuts,
        tabJumping,
    ]);
};
exports.useNavigationHandler = useNavigationHandler;
//# sourceMappingURL=useNavigationHandler.js.map
