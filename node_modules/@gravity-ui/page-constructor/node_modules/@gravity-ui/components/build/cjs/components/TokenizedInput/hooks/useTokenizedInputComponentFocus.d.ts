import { TokenValueBase, TokenizedInputFocusInfo } from "../types.js";
type Props<T extends TokenValueBase = TokenValueBase> = {
    focusInfo: TokenizedInputFocusInfo<T>;
    onFocus?: () => void;
    onBlur?: () => void;
};
export declare const useTokenizedInputComponentFocus: <T extends TokenValueBase = TokenValueBase>({ focusInfo, onFocus, onBlur, }: Props<T>) => void;
export {};
