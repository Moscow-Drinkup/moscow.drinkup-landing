import { jsx as _jsx } from "react/jsx-runtime";
import * as React from 'react';
import { ActionTooltip, Button, Icon } from '@gravity-ui/uikit';
import { block } from "../utils/cn.js";
import "./Notification.css";
const b = block('notification');
export const NotificationAction = React.memo(function NotificationAction({ action }) {
    const content = renderContent(action);
    const button = (_jsx(Button, { qa: action.qa, className: b('action', { icon: Boolean(action.icon) }), view: action.view ?? 'flat', href: action.href, target: action.target, onClick: action.onClick, "aria-label": action.text, children: content }));
    return action.icon ? _jsx(ActionTooltip, { title: action.text, children: button }) : button;
});
function renderContent(action) {
    return action.icon ? _jsx(Icon, { data: action.icon }) : action.text;
}
//# sourceMappingURL=NotificationAction.js.map
