import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import * as React from 'react';
import { Spin } from '@gravity-ui/uikit';
import { block } from "../../../../utils/cn.js";
import { GalleryFallbackText } from "../../FallbackText/index.js";
import { i18n } from "./i18n/index.js";
import "./VideoView.css";
const cnVideoView = block('gallery-video-view');
export const VideoView = ({ className, src, autoPlay = true, controls = true }) => {
    const [status, setStatus] = React.useState('loading');
    const [errorMessage, setErrorMessage] = React.useState();
    const { t } = i18n.useTranslation();
    const handleCanPlay = React.useCallback(() => {
        setStatus('playing');
    }, []);
    const handleError = React.useCallback((event) => {
        setStatus('error');
        const error = event.target.error;
        setErrorMessage(error?.MEDIA_ERR_SRC_NOT_SUPPORTED ? t('not-supported-video') : t('video-error'));
    }, [t]);
    if (status === 'error') {
        return _jsx(GalleryFallbackText, { children: errorMessage });
    }
    return (_jsxs(React.Fragment, { children: [status === 'loading' && _jsx(Spin, { className: cnVideoView('spin'), size: "xl" }), _jsx("video", { className: cnVideoView(null, className), src: src, controls: status === 'loading' ? false : controls, autoPlay: autoPlay, onCanPlay: handleCanPlay, onError: status === 'playing' ? undefined : handleError })] }));
};
//# sourceMappingURL=VideoView.js.map
