import { jsx as _jsx } from "react/jsx-runtime";
import * as React from 'react';
import { Alert } from '@gravity-ui/uikit';
import { i18n } from "../../i18n/index.js";
export function ErrorContainer({ onRetryClick, error }) {
    const { t } = i18n.useTranslation();
    const { title, description } = React.useMemo(() => {
        return error && typeof error === 'object' ? error : {};
    }, [error]);
    return (_jsx(Alert, { title: title || t('label_error-title'), message: description, theme: 'danger', actions: onRetryClick && [
            {
                text: t('button_retry'),
                handler: onRetryClick,
            },
        ] }));
}
//# sourceMappingURL=ErrorContainer.js.map
