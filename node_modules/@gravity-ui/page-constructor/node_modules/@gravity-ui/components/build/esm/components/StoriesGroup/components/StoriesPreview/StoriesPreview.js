import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import * as React from 'react';
import { ChevronLeft, ChevronRight } from '@gravity-ui/icons';
import { Button, Icon } from '@gravity-ui/uikit';
import { MediaRenderer } from "../../../Stories/components/index.js";
import { block } from "../../../utils/cn.js";
import "./StoriesPreview.css";
const PREVIEW_ITEM_SIZE = 40;
const PREVIEW_LIST_GAP = 8;
const b = block('stories-group-preview');
const PreviewItem = ({ active, disabled, groupIndex, media, onSelectGroup }) => {
    const onClick = React.useCallback((event) => {
        event.preventDefault();
        event.stopPropagation();
        onSelectGroup?.(groupIndex);
    }, [onSelectGroup, groupIndex]);
    return (
    // eslint-disable-next-line jsx-a11y/click-events-have-key-events, jsx-a11y/no-static-element-interactions
    _jsx("div", { className: b('stories-preview-item', {
            active,
            disabled,
        }), onClick: onClick, children: media && _jsx(MediaRenderer, { media: media }) }));
};
const StoriesPreviewList = ({ groupIndex, groups, onGroupSelect, offset = 0, }) => {
    return (_jsx(React.Fragment, { children: groups.map(({ thumbnailMedia, items }, i) => {
            const media = thumbnailMedia || items[0]?.media;
            const currentGroupIndex = offset + i;
            return (_jsx(PreviewItem, { groupIndex: currentGroupIndex, active: currentGroupIndex === groupIndex, disabled: items.length === 0, media: media, onSelectGroup: onGroupSelect }, i));
        }) }));
};
const StoriesPreviewListWithSlider = ({ groupIndex, groups, onGroupSelect, maxSliderItemsCount, }) => {
    const [offset, setOffset] = React.useState(0);
    React.useEffect(() => {
        const currentOffset = maxSliderItemsCount * Math.floor(groupIndex / maxSliderItemsCount);
        setOffset(currentOffset);
    }, [groupIndex, maxSliderItemsCount]);
    const setPreviewOffset = React.useCallback((event) => {
        event.preventDefault();
        event.stopPropagation();
        setOffset((currentOffset) => {
            return currentOffset - maxSliderItemsCount;
        });
    }, [maxSliderItemsCount]);
    const setNextOffset = React.useCallback((event) => {
        event.preventDefault();
        event.stopPropagation();
        setOffset((currentOffset) => {
            return currentOffset + maxSliderItemsCount;
        });
    }, [maxSliderItemsCount]);
    return (_jsxs(React.Fragment, { children: [groups.length > maxSliderItemsCount && (_jsx("div", { className: b('slider-button-wrapper'), children: offset !== 0 && (_jsx(Button, { view: "raised", pin: "circle-circle", onClick: setPreviewOffset, children: _jsx(Icon, { data: ChevronLeft }) })) })), _jsx("div", { className: b('slider-preview-list-wrapper'), style: {
                    width: `${PREVIEW_ITEM_SIZE * maxSliderItemsCount +
                        PREVIEW_LIST_GAP * (maxSliderItemsCount - 1)}px`,
                }, children: _jsx(StoriesPreviewList, { groupIndex: groupIndex, groups: groups.slice(offset, offset + maxSliderItemsCount), onGroupSelect: onGroupSelect, offset: offset }) }), groups.length > maxSliderItemsCount && (_jsx("div", { className: b('slider-button-wrapper'), children: offset < groups.length - maxSliderItemsCount && (_jsx(Button, { view: "raised", pin: "circle-circle", onClick: setNextOffset, children: _jsx(Icon, { data: ChevronRight }) })) }))] }));
};
// all onClick handlers of StoriesPreview childrens should have preventDefault() and stopPropagation() callings
export const StoriesPreview = ({ groups, groupIndex, onGroupSelect, maxSliderItemsCount, onClose, }) => {
    const handleClose = React.useCallback((event) => {
        onClose?.(event, 'outsideClick');
    }, [onClose]);
    if (groups.length < maxSliderItemsCount) {
        return (
        // eslint-disable-next-line jsx-a11y/click-events-have-key-events, jsx-a11y/no-static-element-interactions
        _jsx("div", { className: b(), onClick: handleClose, children: _jsx(StoriesPreviewList, { groupIndex: groupIndex, groups: groups, onGroupSelect: onGroupSelect }) }));
    }
    return (
    // eslint-disable-next-line jsx-a11y/click-events-have-key-events, jsx-a11y/no-static-element-interactions
    _jsx("div", { className: b(), onClick: handleClose, children: _jsx(StoriesPreviewListWithSlider, { maxSliderItemsCount: maxSliderItemsCount, groupIndex: groupIndex, groups: groups, onGroupSelect: onGroupSelect }) }));
};
//# sourceMappingURL=StoriesPreview.js.map
