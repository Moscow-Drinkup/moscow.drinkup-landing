import { jsxs as _jsxs, jsx as _jsx } from "react/jsx-runtime";
import * as React from 'react';
import { Button, Flex, List, Sheet } from '@gravity-ui/uikit';
import { block } from "../../../utils/cn.js";
import { i18n } from "../../i18n/index.js";
import "./MobileGalleryActions.css";
const cnMobileGalleryActions = block('mobile-gallery-actions');
export const MobileGalleryActions = ({ open, actions = [], onClose }) => {
    const { t } = i18n.useTranslation();
    const renderListItem = React.useCallback((item) => {
        const title = item.__titleT ? item.__titleT({ t }) : item.title;
        const buttonProps = {
            type: 'button',
            size: 'xl',
            view: 'flat',
            onClick: item.onClick,
            href: item.href,
            target: '__blank',
            'aria-label': title,
            className: cnMobileGalleryActions('list-item'),
            width: 'max',
            children: (_jsxs(Flex, { alignItems: "center", gap: 3, className: cnMobileGalleryActions('custom-item'), children: [item.icon, title] })),
        };
        const render = () => {
            if (item.__renderT) {
                return (_jsx(React.Fragment, { children: item.__renderT(buttonProps, { t }) }, item.id));
            }
            if (item.render) {
                return (_jsx(React.Fragment, { children: item.render(buttonProps) }, item.id));
            }
            return null;
        };
        return render() ?? _jsx(Button, { ...buttonProps }, item.id);
    }, [t]);
    return (_jsx(Sheet, { className: cnMobileGalleryActions(), visible: open, onClose: onClose, children: _jsx(List, { items: actions, filterable: false, renderItem: renderListItem, itemHeight: 44, virtualized: false, onItemClick: onClose }) }));
};
//# sourceMappingURL=MobileGalleryActions.js.map
