import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import * as React from 'react';
import { FieldPopup } from "../Field/FieldPopup.js";
import { SuggestionsList } from "./SuggestionsList.js";
import { useSuggestions } from "./hooks/index.js";
export function Suggestions({ List = SuggestionsList, withPopup = true, ...props }) {
    const suggestionsInfo = useSuggestions(props);
    const { suggestions, isLoading, selected, isPopupOpened, inputElement, popupWidth, popupOffset, fullWidthSuggestions, size, } = suggestionsInfo.state;
    const { onApplySuggestion } = suggestionsInfo.callbacks;
    const renderHint = () => {
        if (!suggestions.hint || isLoading) {
            return null;
        }
        return suggestions.hint;
    };
    const renderList = () => {
        return (_jsx(List, { suggestions: suggestions, isLoading: isLoading, onApplySuggestion: onApplySuggestion, selected: selected, fullWidth: fullWidthSuggestions, size: size }));
    };
    if (withPopup) {
        return (_jsxs(FieldPopup, { anchorElement: inputElement, placement: ['bottom-start', 'top-start'], strategy: 'absolute', offset: popupOffset, open: isPopupOpened, style: { width: popupWidth }, children: [renderHint(), renderList()] }));
    }
    return (_jsxs(React.Fragment, { children: [renderHint(), renderList()] }));
}
//# sourceMappingURL=Suggestions.js.map
