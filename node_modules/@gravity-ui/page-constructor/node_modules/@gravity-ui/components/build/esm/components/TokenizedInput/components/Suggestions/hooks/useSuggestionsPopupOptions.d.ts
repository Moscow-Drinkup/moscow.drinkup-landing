import type { PopupOffset } from '@gravity-ui/uikit';
export declare const useSuggestionsPopupOptions: (inputElement: HTMLInputElement | null) => {
    popupWidth: number | undefined;
    popupOffset: PopupOffset;
};
