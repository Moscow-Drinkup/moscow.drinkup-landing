import type { TokenValueBase } from "../../../types.js";
import type { SuggestionsNavigationOptions } from "../types.js";
type UseSuggestionsNavigationOptions<T extends TokenValueBase> = Omit<SuggestionsNavigationOptions<T>, 'event' | 'value' | 'focus'> & {
    inputElement: HTMLInputElement | null;
    onKeyDown?: (v: SuggestionsNavigationOptions<T>) => boolean;
};
export declare const useSuggestionsNavigation: <T extends TokenValueBase>({ inputElement, onKeyDown, onSelectNext, onSelectPrev, onApply, suggestion, currentWord, }: UseSuggestionsNavigationOptions<T>) => void;
export {};
