import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useTokenList } from "./useTokenList.js";
export function TokenList() {
    const { Token, tokens, newTokenIdx, classNames } = useTokenList();
    return (_jsxs("div", { className: classNames.wrapper, children: [tokens.map((token, idx) => {
                if (token.kind === 'new') {
                    return null;
                }
                return _jsx(Token, { idx: idx }, token.id);
            }), _jsx(Token, { idx: newTokenIdx, kind: "new" }, `newToken-${newTokenIdx}`)] }));
}
//# sourceMappingURL=TokenList.js.map
