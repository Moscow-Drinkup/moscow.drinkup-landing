import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import * as React from 'react';
import { ThemeProvider } from '@gravity-ui/uikit';
import { GalleryItem } from "../../components/index.js";
import { AsyncGallery } from "./AsyncGallery.js";
import { GalleryContext } from "./GalleryContext.js";
export const GalleryProvider = ({ children, container, emptyMessage, theme, className, }) => {
    const [isOpen, setIsOpen] = React.useState(false);
    const [{ items, initialItemIndex }, setGalleryProps] = React.useState({ items: [], initialItemIndex: 0 });
    const contextValue = React.useMemo(() => ({
        openGallery: (items, initialFileIndex) => {
            setGalleryProps({
                items,
                initialItemIndex: initialFileIndex ?? 0,
            });
            setIsOpen(true);
        },
        closeGallery: () => {
            setIsOpen(false);
        },
        open: isOpen,
    }), [isOpen]);
    return (_jsxs(GalleryContext.Provider, { value: contextValue, children: [children, isOpen && (_jsx(React.Suspense, { fallback: null, children: _jsx(ThemeProvider, { theme: theme, children: _jsx(AsyncGallery, { open: isOpen, onOpenChange: setIsOpen, container: container, className: className, emptyMessage: emptyMessage, initialItemIndex: initialItemIndex, children: items.map((file, index) => (_jsx(GalleryItem, { ...file }, index))) }) }) }))] }));
};
//# sourceMappingURL=GalleryProvider.js.map
