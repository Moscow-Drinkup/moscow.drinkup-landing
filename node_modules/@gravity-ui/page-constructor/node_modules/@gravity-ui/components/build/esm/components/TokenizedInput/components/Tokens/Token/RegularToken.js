import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Xmark } from '@gravity-ui/icons';
import i18n from "../../../i18n/index.js";
import { useRegularToken } from "./hooks/index.js";
export function RegularToken({ idx }) {
    const regularTokenInfo = useRegularToken(idx);
    const { token, fields, showRemoveButton, Field, classNames, isEditable } = regularTokenInfo.state;
    const { onChangeField, onFocusField, onBlur, onRemove, getPlaceholder } = regularTokenInfo.callbacks;
    return (_jsxs("div", { className: classNames.wrapper, children: [fields.map(({ className, key }, index) => {
                return (_jsx(Field, { idx: idx, fieldKey: key, onChange: onChangeField, onFocus: onFocusField, onBlur: onBlur, value: token.value[key] ?? '', readOnly: token.options?.readOnlyFields?.includes(key), className: className, error: token.errors?.[key], placeholder: getPlaceholder(index) }, `${key} ${token.id}`));
            }), showRemoveButton && (_jsx("button", { className: classNames.removeButton, onClick: onRemove, tabIndex: -1, disabled: !isEditable, "aria-label": i18n('remove_token', { index: idx }), children: _jsx(Xmark, {}) }))] }));
}
//# sourceMappingURL=RegularToken.js.map
