import { jsx as _jsx } from "react/jsx-runtime";
import { ArrowDownToLine } from '@gravity-ui/icons';
import { Icon } from '@gravity-ui/uikit';
export function getGalleryItemDownloadAction({ downloadUrl, onClick, }) {
    const handleClick = (event) => {
        event?.stopPropagation();
        onClick?.();
    };
    return {
        id: 'download',
        title: 'download',
        __titleT: ({ t }) => t('download'),
        icon: _jsx(Icon, { data: ArrowDownToLine }),
        href: downloadUrl,
        onClick: handleClick,
    };
}
//# sourceMappingURL=getGalleryItemDownloadAction.js.map
