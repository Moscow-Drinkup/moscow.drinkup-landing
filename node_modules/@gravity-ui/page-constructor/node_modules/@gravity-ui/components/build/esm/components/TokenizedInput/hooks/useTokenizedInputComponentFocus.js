import * as React from 'react';
export const useTokenizedInputComponentFocus = ({ focusInfo, onFocus, onBlur, }) => {
    const lastFocused = React.useRef(null);
    React.useEffect(() => {
        const p = lastFocused.current;
        const n = focusInfo.state.focus?.idx;
        const pEmpty = !p && p !== 0;
        const nEmpty = !n && n !== 0;
        if (pEmpty && !nEmpty) {
            onFocus?.();
        }
        else if (!pEmpty && nEmpty) {
            onBlur?.();
        }
        lastFocused.current = n ?? null;
    }, [focusInfo.state.focus?.idx, onBlur, onFocus]);
};
//# sourceMappingURL=useTokenizedInputComponentFocus.js.map
