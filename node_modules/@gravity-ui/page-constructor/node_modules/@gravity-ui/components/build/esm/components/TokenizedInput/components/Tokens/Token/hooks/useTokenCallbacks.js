import * as React from 'react';
import { useFocusContext, useInputContext } from "../../../../context/index.js";
export const useTokenCallbacks = () => {
    const inputInfo = useInputContext();
    const focusInfo = useFocusContext();
    const { onChangeToken } = inputInfo.callbacks;
    const { onFocus } = focusInfo.callbacks;
    const onChangeField = React.useCallback((idx, key, value) => {
        onChangeToken(idx, {
            [key]: value,
        });
    }, [onChangeToken]);
    const onFocusField = React.useCallback((idx, key) => {
        onFocus({
            idx,
            key,
        });
    }, [onFocus]);
    return {
        onChangeField,
        onFocusField,
    };
};
//# sourceMappingURL=useTokenCallbacks.js.map
