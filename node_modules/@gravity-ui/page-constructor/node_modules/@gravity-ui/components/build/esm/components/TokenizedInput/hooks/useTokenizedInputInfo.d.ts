import type { TokenValueBase, TokenizedInputData, TokenizedInputInfo } from "../types.js";
type UseTokenizedInputInfoOptions<T extends TokenValueBase> = Omit<TokenizedInputData<T>, 'onSuggest' | 'onKeyDown' | 'debounceDelay' | 'autoFocus'>;
export declare const useTokenizedInputInfo: <T extends TokenValueBase>({ defaultTokens, isEditable, isClearable, transformTokens, validateToken, formatToken, tokens: externalTokens, tokenErrors, fields, placeholder, className, size, onChange, }: UseTokenizedInputInfoOptions<T>) => TokenizedInputInfo<T>;
export {};
