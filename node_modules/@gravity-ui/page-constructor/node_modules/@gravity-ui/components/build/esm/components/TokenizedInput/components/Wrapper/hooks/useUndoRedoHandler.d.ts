import * as React from 'react';
import type { TokenValueBase } from "../../../types.js";
import type { ShortcutMap } from "./useShortcuts.js";
type UseUndoRedoHandlerOptions = {
    shortcuts: ShortcutMap;
};
export declare const useUndoRedoHandler: <T extends TokenValueBase>({ shortcuts, }: UseUndoRedoHandlerOptions) => (e: React.KeyboardEvent) => boolean;
export {};
