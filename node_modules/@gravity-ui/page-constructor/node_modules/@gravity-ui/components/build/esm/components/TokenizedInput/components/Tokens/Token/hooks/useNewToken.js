import * as React from 'react';
import { b } from "../../../../constants.js";
import { useFocusContext, useInputContext, useTokenizedInputComponents } from "../../../../context/index.js";
import { getDefaultTokenValue } from "../../../../utils.js";
import { useTokenCallbacks } from "./useTokenCallbacks.js";
export const useNewToken = (idx) => {
    const inputInfo = useInputContext();
    const focusInfo = useFocusContext();
    const { Field } = useTokenizedInputComponents();
    const { tokens, fields, placeholder } = inputInfo.state;
    const { autoFocus } = focusInfo.state;
    const { onChangeField, onFocusField } = useTokenCallbacks();
    const token = React.useMemo(() => tokens[idx] ?? { id: 'new-token', value: getDefaultTokenValue(fields), kind: 'new' }, [fields, idx, tokens]);
    const checkIsHidden = React.useCallback((i) => i > 0 && !token.value[fields[i - 1].key], [fields, token.value]);
    const checkIsAutoFocus = React.useCallback((i) => i === 0 && autoFocus, [autoFocus]);
    const getPlaceholder = React.useCallback((i) => {
        if (typeof placeholder === 'function') {
            return placeholder('new', token.value, i);
        }
        return i === 0 ? placeholder : undefined;
    }, [placeholder, token]);
    const classNames = React.useMemo(() => ({
        wrapper: b('token-wrapper', { new: true }),
    }), []);
    return React.useMemo(() => ({
        state: {
            token,
            fields,
            Field,
            classNames,
        },
        callbacks: {
            onChangeField,
            onFocusField,
            getPlaceholder,
            checkIsHidden,
            checkIsAutoFocus,
        },
    }), [
        Field,
        checkIsAutoFocus,
        checkIsHidden,
        classNames,
        fields,
        getPlaceholder,
        onChangeField,
        onFocusField,
        token,
    ]);
};
//# sourceMappingURL=useNewToken.js.map
