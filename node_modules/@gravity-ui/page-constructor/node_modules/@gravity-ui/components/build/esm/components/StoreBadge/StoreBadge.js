import { jsx as _jsx } from "react/jsx-runtime";
import { Icon, Lang, Link } from '@gravity-ui/uikit';
import { AppStoreEn } from "../../icons/stores/AppStoreEn.js";
import { AppStoreRu } from "../../icons/stores/AppStoreRu.js";
import { GooglePlayEn } from "../../icons/stores/GooglePlayEn.js";
import { GooglePlayRu } from "../../icons/stores/GooglePlayRu.js";
import { cn } from "../utils/cn.js";
const b = cn('store-badge');
const androidPlatform = 'android';
const iosPlatform = 'ios';
const badgeData = {
    [iosPlatform]: {
        ru: AppStoreRu,
        en: AppStoreEn,
    },
    [androidPlatform]: {
        ru: GooglePlayRu,
        en: GooglePlayEn,
    },
};
const badgeLabelData = {
    [iosPlatform]: {
        ru: 'Скачать в App Store',
        en: 'Download on the App Store',
    },
    [androidPlatform]: {
        ru: 'Скачать из Google Play',
        en: 'Get it on Google Play',
    },
};
export const StoreBadge = ({ platform, className, onClick, href, lang = Lang.En, ...restLinkProps }) => {
    // const lang = i18n.lang as Lang;
    const iconData = badgeData?.[platform][lang];
    const iconLabelData = badgeLabelData?.[platform][lang];
    if (!iconData) {
        return null;
    }
    if (!href) {
        return (
        // eslint-disable-next-line jsx-a11y/click-events-have-key-events, jsx-a11y/interactive-supports-focus
        _jsx("div", { role: "button", onClick: onClick, "aria-label": iconLabelData, children: _jsx(Icon, { className: b(null, className), data: iconData }) }));
    }
    return (_jsx(Link, { className: b(null, className), onClick: onClick, href: href, target: "_blank", rel: "noopener", ...restLinkProps, extraProps: {
            'aria-label': iconLabelData,
            ...restLinkProps.extraProps,
        }, children: _jsx(Icon, { data: iconData }) }));
};
//# sourceMappingURL=StoreBadge.js.map
