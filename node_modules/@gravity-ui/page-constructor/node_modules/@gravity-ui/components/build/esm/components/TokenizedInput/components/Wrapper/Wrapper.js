import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Xmark } from '@gravity-ui/icons';
import { Icon } from '@gravity-ui/uikit';
import i18n from "../../i18n/index.js";
import { useWrapper } from "./hooks/index.js";
export function Wrapper({ children }) {
    const wrapperInfo = useWrapper();
    const { isClearable, classNames, wrapperRef } = wrapperInfo.state;
    const { onBlur, onKeyDown, onClear } = wrapperInfo.callbacks;
    // Narrows RefObject<T | null> back to RefObject<T>
    // for compatibility with @types/react@18 LegacyRef typing.
    const divRef = wrapperRef;
    return (_jsxs("div", { className: classNames.wrapper, onBlur: onBlur, onKeyDown: onKeyDown, ref: divRef, children: [children, isClearable && (_jsx("button", { className: classNames.clearButton, onClick: onClear, tabIndex: -1, "aria-label": i18n('clear_input'), title: i18n('clear_input'), children: _jsx(Icon, { data: Xmark }) }))] }));
}
//# sourceMappingURL=Wrapper.js.map
