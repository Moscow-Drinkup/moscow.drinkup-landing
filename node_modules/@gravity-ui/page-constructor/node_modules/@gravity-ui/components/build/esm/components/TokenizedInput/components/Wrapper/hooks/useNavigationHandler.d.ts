import * as React from 'react';
import type { TokenValueBase } from "../../../types.js";
import type { ShortcutMap } from "./useShortcuts.js";
type UseNavigationHandlerOptions = {
    shortcuts: ShortcutMap;
    checkKey: (e: React.KeyboardEvent, key: string) => boolean;
    getCursorOffset: (input: HTMLInputElement) => number | undefined;
};
export declare const useNavigationHandler: <T extends TokenValueBase>({ shortcuts, checkKey, getCursorOffset, }: UseNavigationHandlerOptions) => (e: React.KeyboardEvent) => boolean;
export {};
