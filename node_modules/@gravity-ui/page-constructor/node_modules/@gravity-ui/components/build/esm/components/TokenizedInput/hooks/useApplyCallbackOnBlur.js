import * as React from 'react';
import { useOptionsContext } from "../context/index.js";
export const useApplyCallbackOnBlur = (fn) => {
    const { shouldAllowBlur } = useOptionsContext();
    return React.useCallback((e) => {
        if (!e.currentTarget.contains(e.relatedTarget) && shouldAllowBlur?.(e)) {
            fn(e);
        }
    }, [fn, shouldAllowBlur]);
};
//# sourceMappingURL=useApplyCallbackOnBlur.js.map
