import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import * as React from 'react';
import { Spin, useMobile } from '@gravity-ui/uikit';
import { block } from "../../../../utils/cn.js";
import { useGalleryContext } from "../../../contexts/GalleryContext/index.js";
import { useImageZoom } from "../../../hooks/useImageZoom/index.js";
import { GalleryFallbackText } from "../../FallbackText/index.js";
import "./ImageView.css";
const cnImageView = block('gallery-image-view');
export const ImageView = ({ className, src, alt = '' }) => {
    const [status, setStatus] = React.useState('loading');
    const imageRef = React.useRef(null);
    const containerRef = React.useRef(null);
    const isMobile = useMobile();
    const { onTap, onViewInteractionChange } = useGalleryContext();
    const { imageHandlers, setImageSize, setContainerSize, resetZoom, imageStyles, isZooming } = useImageZoom({ onTap });
    React.useEffect(() => {
        onViewInteractionChange(isZooming);
    }, [isZooming, onViewInteractionChange]);
    const handleLoad = React.useCallback(() => {
        setStatus('complete');
        if (imageRef.current) {
            setImageSize({
                width: imageRef.current.naturalWidth,
                height: imageRef.current.naturalHeight,
            });
        }
    }, [setImageSize]);
    const handleError = React.useCallback(() => {
        setStatus('error');
    }, []);
    // Track container dimensions and handle resize
    React.useEffect(() => {
        if (!containerRef.current)
            return undefined;
        const updateSize = () => {
            if (containerRef.current) {
                const size = {
                    width: containerRef.current.clientWidth,
                    height: containerRef.current.clientHeight,
                };
                // Only update if dimensions are valid
                if (size.width > 0 && size.height > 0) {
                    setContainerSize(size);
                }
            }
        };
        const resizeObserver = new ResizeObserver(() => {
            updateSize();
        });
        resizeObserver.observe(containerRef.current);
        updateSize();
        const timeoutId = setTimeout(updateSize, 100);
        window.addEventListener('resize', updateSize);
        return () => {
            resizeObserver.disconnect();
            clearTimeout(timeoutId);
            window.removeEventListener('resize', updateSize);
        };
    }, [setContainerSize]);
    React.useEffect(() => {
        resetZoom();
    }, [src, resetZoom]);
    if (status === 'error') {
        return _jsx(GalleryFallbackText, {});
    }
    return (_jsxs("div", { ref: containerRef, className: cnImageView({ mobile: isMobile }, className), children: [status === 'loading' && _jsx(Spin, { className: cnImageView('spin'), size: "xl" }), _jsx("img", { ref: imageRef, className: cnImageView('image'), src: src, alt: alt, ...imageHandlers, onLoad: handleLoad, onError: handleError, style: imageStyles })] }));
};
//# sourceMappingURL=ImageView.js.map
