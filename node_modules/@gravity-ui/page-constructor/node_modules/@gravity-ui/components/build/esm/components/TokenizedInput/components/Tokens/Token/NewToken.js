import { jsx as _jsx } from "react/jsx-runtime";
import { useNewToken } from "./hooks/index.js";
export function NewToken({ idx }) {
    const newTokenInfo = useNewToken(idx);
    const { token, fields, Field, classNames } = newTokenInfo.state;
    const { onChangeField, onFocusField, checkIsHidden, checkIsAutoFocus, getPlaceholder } = newTokenInfo.callbacks;
    return (_jsx("div", { className: classNames.wrapper, children: fields.map(({ key }, i) => {
            return (_jsx(Field, { idx: idx, fieldKey: key, placeholder: getPlaceholder(i), onChange: onChangeField, onFocus: onFocusField, hidden: checkIsHidden(i), value: token.value[key] ?? '', autoFocus: checkIsAutoFocus(i), isNew: true }, `${key}new`));
        }) }));
}
//# sourceMappingURL=NewToken.js.map
