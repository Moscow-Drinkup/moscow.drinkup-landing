import * as React from 'react';
export type UseNavigationProps = {
    initialItemIndex?: number;
    activeItemIndex?: number;
    onActiveItemIndexChange?: (index: number) => void;
    itemRefs: React.RefObject<HTMLButtonElement | null>[];
    keyboardScope?: React.RefObject<HTMLElement | null>;
};
export declare function useNavigation({ initialItemIndex, activeItemIndex: controlledActiveItemIndex, onActiveItemIndexChange, itemRefs, keyboardScope, }: UseNavigationProps): {
    activeItemIndex: number;
    handleGoToPrevious: () => void;
    handleGoToNext: () => void;
    setActiveItemIndex: (update: number | ((previousActiveItemIndex: number) => number)) => void;
};
