import type { TokenValueBase, TokenizedSuggestions, TokenizedSuggestionsItem } from "../../../types.js";
import { SuggestionsData } from "../types.js";
type UseSuggestionsOptions<T extends TokenValueBase> = SuggestionsData<T> & {
    inputElement: HTMLInputElement | null;
};
export declare const useSuggestions: <T extends TokenValueBase>({ idx, fieldKey, value, offset, selection, inputElement, onKeyDown, }: UseSuggestionsOptions<T>) => {
    state: {
        suggestions: TokenizedSuggestions<T>;
        isLoading: boolean;
        selected: number;
        isPopupOpened: boolean;
        inputElement: HTMLInputElement | null;
        popupWidth: number | undefined;
        popupOffset: import("@gravity-ui/uikit").PopupOffset;
        fullWidthSuggestions: boolean;
        size: import("../../../index.js").TokenizedInputSize | undefined;
    };
    callbacks: {
        onApplySuggestion: (suggestion: TokenizedSuggestionsItem<T>) => void;
    };
};
export {};
