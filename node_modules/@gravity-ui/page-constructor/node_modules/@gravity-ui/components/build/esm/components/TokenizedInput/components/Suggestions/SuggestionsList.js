import { jsx as _jsx } from "react/jsx-runtime";
import * as React from 'react';
import { List } from '@gravity-ui/uikit';
import { b } from "../../constants.js";
import i18n from "../../i18n/index.js";
export function SuggestionsList({ selected, isLoading, suggestions, fullWidth, size = 'm', onApplySuggestion, }) {
    const { items, options, currentWord } = suggestions;
    const showEmptyState = options?.showEmptyState !== false;
    const isEmpty = !isLoading && items.length === 0;
    const currentText = currentWord?.value ?? '';
    const EmptyPlaceholder = React.useMemo(() => {
        if (isLoading || !showEmptyState) {
            return null;
        }
        return (_jsx("p", { className: b('suggestions-list-item', { default: true }), children: i18n('suggestions.items_not_found', { text: currentText }) }));
    }, [currentText, isLoading, showEmptyState]);
    if (isEmpty && !showEmptyState) {
        return null;
    }
    return (_jsx("div", { className: b('suggestions-list-wrapper', {
            empty: isEmpty && showEmptyState,
            loading: isLoading,
            'full-width': fullWidth,
            size,
        }), children: _jsx(List, { items: items, renderItem: (s) => _jsx("div", { className: b('suggestions-list-item'), children: s.label }), activeItemIndex: selected, filterable: false, loading: isLoading, emptyPlaceholder: EmptyPlaceholder, onItemClick: onApplySuggestion, virtualized: false }) }));
}
//# sourceMappingURL=SuggestionsList.js.map
