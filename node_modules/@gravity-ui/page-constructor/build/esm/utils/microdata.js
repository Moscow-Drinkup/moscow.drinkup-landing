import sanitize from 'sanitize-html';
export const mergeVideoMicrodata = (values = {}, newValues = {}) => ({ ...values, videoMicrodata: { ...newValues, ...(values.videoMicrodata || {}) } });
export function sanitizeMicrodata(html) {
    return html && sanitize(html, { allowedTags: [], allowedAttributes: {} });
}
//# sourceMappingURL=microdata.js.map
