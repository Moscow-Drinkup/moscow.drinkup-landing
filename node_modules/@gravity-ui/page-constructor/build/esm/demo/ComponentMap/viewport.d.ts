import type { Point, Rect, ViewportSize, ViewportTransform } from "./model.js";
export declare const VIEWPORT_PADDING = 24;
export declare const MINIMUM_VIEWPORT_SCALE = 0.01;
export declare const worldToScreen: (point: Point, transform: ViewportTransform) => Point;
export declare const screenToWorld: (point: Point, transform: ViewportTransform) => Point;
export declare function fitToViewport(bounds: Rect, viewport: ViewportSize, padding?: number): ViewportTransform;
export declare function zoomAtPoint(transform: ViewportTransform, factor: number, point: Point, minScale: number, maxScale: number): ViewportTransform;
