import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import * as React from 'react';
import { block } from "../../../utils/index.js";
import { getBlockId } from "../../utils/index.js";
import { i18n } from "./i18n/index.js";
import './ErrorBoundary.css';
const b = block('error-boundary');
export class ErrorBoundary extends React.Component {
    state = {};
    componentDidCatch(error, info) {
        this.setState({ error: `${error.message} ${info?.componentStack}` });
    }
    render() {
        const { type, index } = this.props;
        const { error } = this.state;
        const header = type
            ? i18n('error-block-header', { id: getBlockId({ type, index }) })
            : i18n('error-page-header');
        if (this.state.error) {
            return (_jsx("div", { className: b(), children: _jsxs("div", { className: b('container'), children: [_jsx("div", { className: b('image') }), _jsxs("div", { className: b('content'), children: [_jsx("h2", { className: b('header'), children: header }), _jsx("code", { className: b('error'), children: error })] })] }) }));
        }
        return this.props.children;
    }
}
//# sourceMappingURL=ErrorBoundary.js.map
