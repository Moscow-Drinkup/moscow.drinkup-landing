import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import * as React from 'react';
import { block } from "../../../utils/index.js";
import { EditModeItem } from "../../types/index.js";
import DeviceEmulation from "../DeviceEmulation/DeviceEmulation.js";
import './Layout.css';
const b = block('editor-layout');
const Left = () => null;
const Right = () => null;
// TODO in https://github.com/gravity-ui/page-constructor/issues/884 this component will be disappeared
const Layout = ({ children, editMode, viewMode }) => {
    let left, right;
    const isEditingMode = editMode === EditModeItem.Form;
    function handleChild(child) {
        switch (child?.type) {
            case Left:
                left = child?.props.children;
                break;
            case Right:
                right = child?.props.children;
                break;
        }
    }
    if (React.Children.toArray(children).length) {
        React.Children.forEach(children, handleChild);
    }
    return (_jsx("div", { className: b('container'), children: _jsxs(React.Fragment, { children: [left && _jsx("div", { className: b('left'), children: left }), right && (_jsx("div", { className: b('right', { editing: isEditingMode }), children: _jsx(DeviceEmulation, { mode: viewMode, children: right }) }))] }) }));
};
Layout.Left = Left;
Layout.Right = Right;
export default Layout;
//# sourceMappingURL=Layout.js.map
