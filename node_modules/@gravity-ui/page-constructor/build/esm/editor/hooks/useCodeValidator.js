import * as React from 'react';
import { createValidator, validate } from "../utils/validation.js";
export function useCodeValidator(schema) {
    const validator = React.useMemo(() => createValidator(schema), [schema]);
    return React.useCallback((code) => validate(code, validator), [validator]);
}
//# sourceMappingURL=useCodeValidator.js.map
