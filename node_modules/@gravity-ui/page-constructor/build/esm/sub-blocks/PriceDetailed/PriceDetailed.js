import { jsx as _jsx } from "react/jsx-runtime";
import * as React from 'react';
import { PriceDetailsType, } from "../../models/index.js";
import CombinedPriceDetailed from "./CombinedPriceDetailed/CombinedPriceDetailed.js";
import PriceDescription from "./PriceDescription/PriceDescription.js";
import PriceDetails from "./PriceDetails/PriceDetails.js";
import SeparatePriceDetailed from "./SeparatePriceDetailed/SeparatePriceDetailed.js";
// eslint-disable-next-line valid-jsdoc
/** @deprecated */
const PriceDetailed = (props) => {
    const { priceType = PriceDetailsType.SETTINGS, items, numberGroupItems = 1, description, details, foldable, labelsDefaultText, isCombined = false, useMixedView = false, border, } = props;
    const { titleSize: descriptionTitleSize = 'l', descriptionSize = 'm', titleColor: descriptionTitleColor = 'cornflower', } = description || {};
    const { titleSize: detailsTitleSize = 's', descriptionSize: detailsDescriptionSize = 'm' } = details || {};
    const getDescriptionComponent = (priceDescription) => {
        return (_jsx(PriceDescription, { ...priceDescription, titleSize: descriptionTitleSize, descriptionSize: descriptionSize, colorTitle: descriptionTitleColor, labelsDefaultText: labelsDefaultText }));
    };
    const getDetailsComponent = (priceDetails) => {
        return (_jsx(PriceDetails, { items: priceDetails, type: priceType, foldable: foldable, titleSize: detailsTitleSize, descriptionSize: detailsDescriptionSize, useMixedView: useMixedView }));
    };
    return (_jsx(React.Fragment, { children: isCombined ? (_jsx(CombinedPriceDetailed, { items: items, numberGroupItems: numberGroupItems, border: border, useMixedView: useMixedView, getDescriptionComponent: getDescriptionComponent, getDetailsComponent: getDetailsComponent })) : (_jsx(SeparatePriceDetailed, { items: items, border: border, getDescriptionComponent: getDescriptionComponent, getDetailsComponent: getDetailsComponent })) }));
};
export default PriceDetailed;
//# sourceMappingURL=PriceDetailed.js.map
