import * as React from 'react';
import { getNavigationItemWithIconSize } from "../utils.js";
const useActiveNavItem = (iconSize, leftItems, rightItems) => {
    const [activeItemId, setActiveItemId] = React.useState(undefined);
    const getNavigationItem = getNavigationItemWithIconSize(iconSize);
    const leftItemsWithIconSize = React.useMemo(() => leftItems.map(getNavigationItem), [getNavigationItem, leftItems]);
    const rightItemsWithIconSize = React.useMemo(() => rightItems?.map(getNavigationItem), [getNavigationItem, rightItems]);
    const onActiveItemChange = (id) => {
        setActiveItemId(id);
    };
    return { activeItemId, leftItemsWithIconSize, rightItemsWithIconSize, onActiveItemChange };
};
export default useActiveNavItem;
//# sourceMappingURL=useActiveNavItem.js.map
