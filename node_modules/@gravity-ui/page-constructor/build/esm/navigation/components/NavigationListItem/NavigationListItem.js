import { jsx as _jsx } from "react/jsx-runtime";
import { getItemClickHandler } from "../../utils.js";
import NavigationItem from "../NavigationItem/index.js";
export const NavigationListItem = ({ column, index, activeItemId, onActiveItemChange, ...props }) => {
    const id = `${column}-${index}`;
    const isActive = id === activeItemId;
    const onClick = getItemClickHandler({
        column,
        index,
        activeItemId,
        onActiveItemChange,
    });
    return (_jsx(NavigationItem, { isActive: isActive, onClick: onClick, hidePopup: onActiveItemChange, ...props }));
};
export default NavigationListItem;
//# sourceMappingURL=NavigationListItem.js.map
