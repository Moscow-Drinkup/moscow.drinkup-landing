import { jsx as _jsx } from "react/jsx-runtime";
import * as icons from '@gravity-ui/icons';
import { Icon as UiKitIcon } from '@gravity-ui/uikit';
import { block } from "../../utils/index.js";
import Image from "../Image/Image.js";
import { getMediaImage } from "../Media/Image/utils.js";
import './Icon.css';
const b = block('icon');
const Icon = ({ icon, className, gravityIcon, containerClassName, qa }) => {
    if (gravityIcon) {
        const data = (typeof gravityIcon === 'string' ? gravityIcon : gravityIcon.name);
        const color = typeof gravityIcon === 'string' ? 'brand' : gravityIcon.color;
        return (_jsx("div", { className: b({ color }), children: _jsx(UiKitIcon, { data: icons[data], className: className, qa: qa }) }));
    }
    if (icon) {
        const image = getMediaImage(icon);
        return (_jsx(Image, { ...image, containerClassName: containerClassName, className: className, qa: qa }));
    }
    return null;
};
export default Icon;
//# sourceMappingURL=Icon.js.map
