import * as React from 'react';
import { ClassNameProps } from "../../models/index.js";
import './ErrorWrapper.css';
export interface ErrorWrapperProps extends ClassNameProps {
    text: string;
    handler: () => void;
    isError: boolean;
    buttonText: string;
    children: React.ReactNode;
}
declare const ErrorWrapper: ({ text, buttonText, className, handler, isError, children, }: React.PropsWithChildren<ErrorWrapperProps>) => import("react/jsx-runtime").JSX.Element;
export default ErrorWrapper;
