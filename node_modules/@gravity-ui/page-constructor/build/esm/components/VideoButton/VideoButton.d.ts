import * as React from 'react';
import { PlayButtonProps } from "../../models/index.js";
import './VideoButton.css';
export interface VideoButtonProps extends PlayButtonProps {
    customButton?: React.ReactNode;
    ref?: React.LegacyRef<HTMLButtonElement>;
    id?: string;
}
declare const VideoButton: ({ customButton, text, type, theme: initialTheme, className, ref, id, }: VideoButtonProps) => string | number | true | Iterable<React.ReactNode> | import("react/jsx-runtime").JSX.Element;
export default VideoButton;
