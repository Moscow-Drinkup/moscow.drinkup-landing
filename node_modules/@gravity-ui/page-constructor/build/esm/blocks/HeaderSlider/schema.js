import omit from "lodash/omit.js";
import { HeaderProperties, SliderProps } from "../../schema/validators/blocks.js";
import { BlockBaseProps } from "../../schema/validators/common.js";
export const HeaderSliderBlock = {
    'header-slider-block': {
        additionalProperties: false,
        required: ['items'],
        properties: {
            ...BlockBaseProps,
            ...omit(SliderProps, ['loadable', 'children']),
            items: {
                type: 'array',
                items: {
                    type: 'object',
                    additionalProperties: false,
                    required: ['title'],
                    properties: HeaderProperties,
                },
            },
        },
    },
};
//# sourceMappingURL=schema.js.map
