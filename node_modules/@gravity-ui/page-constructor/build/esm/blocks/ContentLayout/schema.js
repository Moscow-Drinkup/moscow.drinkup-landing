import { BackgroundImageProps } from "../../components/Image/schema.js";
import { BlockBaseProps, FileLinkProps, contentSizes, contentTextWidth, contentThemes, } from "../../schema/validators/common.js";
import { filteredArray } from "../../schema/validators/utils.js";
import { ContentBlock } from "../../sub-blocks/Content/schema.js";
const ContentLayoutBlockProperties = {
    size: {
        type: 'string',
        enum: contentSizes,
    },
    background: BackgroundImageProps,
    centered: {
        type: 'boolean',
    },
    theme: {
        type: 'string',
        enum: contentThemes,
    },
    textWidth: {
        type: 'string',
        enum: contentTextWidth,
    },
};
export const ContentLayoutBlock = {
    'content-layout-block': {
        additionalProperties: false,
        properties: {
            ...BlockBaseProps,
            textContent: ContentBlock?.content,
            fileContent: filteredArray(FileLinkProps),
            /**
             * @deprecated Use params on top level instead
             */
            properties: ContentLayoutBlockProperties,
            ...ContentLayoutBlockProperties,
        },
    },
};
//# sourceMappingURL=schema.js.map
