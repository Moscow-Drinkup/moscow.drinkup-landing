import omit from "lodash/omit.js";
import { BaseProps, ButtonBlock, LinkProps, ThemeProps, withTheme, } from "../../schema/validators/common.js";
import { filteredArray } from "../../schema/validators/utils.js";
import { ContentBase } from "../../sub-blocks/Content/schema.js";
const ContentProps = {
    additionalProperties: false,
    properties: omit(ContentBase, ['size', 'colSizes', 'theme']),
};
export const InfoBlock = {
    'info-block': {
        additionalProperties: false,
        required: ['title', 'sectionsTitle'],
        properties: {
            ...BaseProps,
            title: {
                type: 'string',
                contentType: 'text',
            },
            backgroundColor: withTheme({
                type: 'string',
            }),
            sectionsTitle: {
                type: 'string',
                contentType: 'text',
            },
            buttons: filteredArray(ButtonBlock),
            theme: ThemeProps,
            links: filteredArray(LinkProps),
            leftContent: ContentProps,
            rightContent: ContentProps,
        },
    },
};
//# sourceMappingURL=schema.js.map
