import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Flex, Link, Tooltip } from '@gravity-ui/uikit';
import { Image } from "../../../../components/index.js";
import { useTheme } from "../../../../context/theme/index.js";
import { Col } from "../../../../grid/index.js";
import { block, getThemedValue } from "../../../../utils/index.js";
import { useLogoImageProps } from "../../hooks/useLogoImageProps.js";
import './SocialFloor.css';
const b = block('footer-block');
const SocialIconItem = ({ contact, theme }) => {
    const iconResolved = contact.icon && getThemedValue(contact.icon, theme);
    const iconProps = useLogoImageProps(iconResolved);
    if (!iconProps) {
        return null;
    }
    const content = (_jsx("li", { className: b('social-icons-item'), children: _jsx(Link, { className: b('social-icon-link'), href: contact.url, target: "_blank", rel: "noopener noreferrer", children: _jsx(Flex, { direction: "column", alignItems: "center", gap: 2, children: _jsx(Image, { ...iconProps, alt: contact.urlTitle ?? '', className: b('social-icon-item') }) }) }) }));
    return contact.urlTitle ? (_jsx(Tooltip, { content: contact.urlTitle, openDelay: 500, children: content })) : (content);
};
const SocialIcons = ({ links, theme }) => {
    if (!links?.length)
        return null;
    return (_jsx("ul", { className: b('social-icons-list'), children: links.map((contact, index) => (_jsx(SocialIconItem, { contact: contact, theme: theme }, index))) }));
};
export const SocialFloor = ({ contacts }) => {
    const theme = useTheme();
    if (!contacts.links?.length) {
        return null;
    }
    return (_jsx(Col, { sizes: { all: 12 }, className: b('floor', { social: true }), children: _jsxs("div", { className: b('social-floor-inner', {
                'title-position': contacts.titlePosition || 'top',
                'links-position': contacts.linksPosition || 'left',
                size: contacts.iconsSize ?? 'l',
            }), children: [contacts.title && _jsx("h3", { className: b('social-floor-title'), children: contacts.title }), _jsx(SocialIcons, { links: contacts.links, theme: theme })] }) }));
};
//# sourceMappingURL=SocialFloor.js.map
