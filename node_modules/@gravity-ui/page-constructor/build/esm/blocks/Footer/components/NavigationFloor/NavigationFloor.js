import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import * as React from 'react';
import { Link } from '@gravity-ui/uikit';
import { Col, Row } from "../../../../grid/index.js";
import { block } from "../../../../utils/index.js";
import './NavigationFloor.css';
const b = block('footer-block');
const DEFAULT_COL_SIZES_WITH_LOGO = { all: 6, sm: 4, md: 3, lg: 3 };
const DEFAULT_COL_SIZES_WITHOUT_LOGO = { all: 6, sm: 4, md: 3, lg: 3 };
const DEFAULT_LOGO_COL_SIZES = { all: 12, md: 4 };
function NavigationColumns({ columns, hasLogo, colSizes, columnsPerRow }) {
    const defaultColSizes = hasLogo
        ? DEFAULT_COL_SIZES_WITH_LOGO
        : DEFAULT_COL_SIZES_WITHOUT_LOGO;
    const resolvedColSizes = React.useMemo(() => ({
        all: colSizes?.all ?? defaultColSizes.all,
        sm: colSizes?.sm ?? defaultColSizes.sm,
        md: colSizes?.md ?? defaultColSizes.md,
        lg: colSizes?.lg ?? defaultColSizes.lg,
    }), [colSizes, defaultColSizes]);
    const customColumnsStyle = columnsPerRow
        ? {
            '--pc-footer-navigation-column-width': `${100 / columnsPerRow}%`,
        }
        : undefined;
    return (_jsx(Col, { className: b('columns-container'), children: _jsx(Row, { className: b('nav-row', { 'custom-columns': Boolean(columnsPerRow) }), style: customColumnsStyle, children: columns.map((column, columnIndex) => (_jsx(Col, { className: b('column'), sizes: resolvedColSizes, children: _jsxs("div", { className: b('column-inner'), children: [_jsx("h6", { className: b('column-title'), children: column.title }), _jsx("ul", { className: b('links'), children: column.links.map((link, linkIndex) => (_jsx("li", { className: b('link-item'), children: _jsx(Link, { href: link.url, className: b('link'), title: link.urlTitle, target: link.target, children: link.text }) }, linkIndex))) })] }) }, columnIndex))) }) }));
}
export const NavigationFloor = ({ columns, logoContent, colSizes, columnsPerRow, logoPlacement, }) => {
    const isTopLogoPlacement = logoPlacement === 'top';
    const hasSideLogo = Boolean(logoContent) && !isTopLogoPlacement;
    return (_jsxs(Row, { className: b('floor', { navigation: true, 'logo-placement': logoPlacement }), children: [logoContent && isTopLogoPlacement && (_jsx(Col, { className: b('logo-col', { top: true }), sizes: { all: 12 }, children: logoContent })), logoContent && !isTopLogoPlacement && (_jsx(Col, { className: b('logo-col'), sizes: DEFAULT_LOGO_COL_SIZES, children: _jsx(Row, { children: logoContent }) })), _jsx(NavigationColumns, { columns: columns, hasLogo: hasSideLogo, colSizes: colSizes, columnsPerRow: columnsPerRow })] }));
};
//# sourceMappingURL=NavigationFloor.js.map
