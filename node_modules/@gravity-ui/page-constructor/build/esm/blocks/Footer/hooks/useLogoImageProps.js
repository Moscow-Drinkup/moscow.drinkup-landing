import * as React from 'react';
import { getMediaImage } from "../../../components/Media/Image/utils.js";
export function useLogoImageProps(logoImage) {
    return React.useMemo(() => {
        if (!logoImage) {
            return null;
        }
        const resolved = getMediaImage(logoImage);
        if (typeof resolved === 'string') {
            return { src: resolved };
        }
        return resolved && typeof resolved === 'object'
            ? resolved
            : null;
    }, [logoImage]);
}
//# sourceMappingURL=useLogoImageProps.js.map
