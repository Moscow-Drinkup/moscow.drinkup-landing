import { jsx as _jsx } from "react/jsx-runtime";
import { Col } from "../../../grid/index.js";
import { Content } from "../../../sub-blocks/index.js";
import { block } from "../../../utils/index.js";
import './TabsTextContent.css';
const b = block('tabs-block-text-content');
export const TabsTextContent = ({ centered, contentSize = 's', showMedia, data, imageProps, isReverse, }) => {
    if (!data) {
        return null;
    }
    const { media, title, text, additionalInfo, link, links, buttons, list } = data;
    return (_jsx(Col, { sizes: { all: 12, md: showMedia ? 4 : 8 }, className: b({ centered: centered }), children: _jsx("div", { className: b('wrapper', {
                reverse: isReverse,
                'no-image': !(media || imageProps),
            }), children: _jsx(Content, { title: title, text: text, additionalInfo: additionalInfo, size: contentSize, list: list, links: [...(link ? [link] : []), ...(links || [])], buttons: buttons, colSizes: { all: 12 } }) }) }));
};
export default TabsTextContent;
//# sourceMappingURL=TabsTextContent.js.map
