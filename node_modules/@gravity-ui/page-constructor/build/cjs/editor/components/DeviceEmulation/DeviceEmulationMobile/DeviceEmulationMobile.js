"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const jsx_runtime_1 = require("react/jsx-runtime");
const React = tslib_1.__importStar(require("react"));
const utils_1 = require("../../../../utils/index.js");
const context_1 = require("../../../context.js");
const widget_1 = require("../../../widget/index.js");
const b = (0, utils_1.block)('device-emulation-mobile');
const DeviceEmulationMobile = ({ device, active }) => {
    const { deviceEmulationSettings, ...initialData } = React.useContext(context_1.EditorContext);
    const containerRef = React.useRef(null);
    const deviceIframeRef = React.useRef(null);
    React.useEffect(() => {
        let iframe;
        if (containerRef?.current) {
            iframe = new widget_1.DeviceIframe(containerRef?.current, {
                initialData,
                className: b('frame', { device }),
                settings: deviceEmulationSettings,
            });
            deviceIframeRef.current = iframe;
        }
        return () => {
            iframe?.destroy();
        };
        // render iframe only once, then update it's data with postMessage
        // eslint-disable-next-line react-hooks/exhaustive-deps
    }, [device]);
    React.useEffect(() => {
        if (deviceIframeRef.current) {
            deviceIframeRef.current.onActivenessUpdate(active);
        }
    }, [active]);
    React.useEffect(() => {
        if (deviceIframeRef.current && initialData) {
            deviceIframeRef.current.onDataUpdate(initialData);
        }
    }, [initialData]);
    return (0, jsx_runtime_1.jsx)("div", { className: b({ active, device }), ref: containerRef });
};
exports.default = DeviceEmulationMobile;
//# sourceMappingURL=DeviceEmulationMobile.js.map
