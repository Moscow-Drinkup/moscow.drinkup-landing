"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ComponentMap = ComponentMap;
const tslib_1 = require("tslib");
const jsx_runtime_1 = require("react/jsx-runtime");
const React = tslib_1.__importStar(require("react"));
const uikit_1 = require("@gravity-ui/uikit");
const cn_1 = require("../../utils/cn.js");
const draw_1 = require("./draw.js");
const edgeGeometry_1 = require("./edgeGeometry.js");
const graph_1 = require("./graph.js");
const layout_1 = require("./layout.js");
const viewport_1 = require("./viewport.js");
const b = (0, cn_1.block)('component-map');
const { useCallback, useEffect, useLayoutEffect, useMemo, useRef, useState } = React;
const TOOLBAR_ZOOM_FACTOR = 1.2;
const WHEEL_ZOOM_SENSITIVITY = 0.002;
const CLICK_MOVEMENT_THRESHOLD = 4;
const NON_PASSIVE_WHEEL_OPTIONS = { passive: false };
const EMPTY_SIZE = { width: 0, height: 0, devicePixelRatio: 1 };
function ComponentMap({ data }) {
    const rootRef = useRef(null);
    const canvasRef = useRef(null);
    const animationFrameRef = useRef(null);
    const pointerDragRef = useRef(null);
    const layout = useMemo(() => (0, layout_1.layoutComponentMap)(data), [data]);
    const [size, setSize] = useState(EMPTY_SIZE);
    const [transform, setTransform] = useState(null);
    const [searchQuery, setSearchQuery] = useState('');
    const [selectedNodeId, setSelectedNodeId] = useState();
    const [selectedEdgeKeys, setSelectedEdgeKeys] = useState(() => new Set());
    const [hoveredNodeId, setHoveredNodeId] = useState();
    const [hoveredEdgeKey, setHoveredEdgeKey] = useState();
    const [dragging, setDragging] = useState(false);
    const fit = useMemo(() => (size.width > 0 && size.height > 0 ? (0, viewport_1.fitToViewport)(layout.bounds, size) : undefined), [layout, size]);
    const emphasis = useMemo(() => (0, graph_1.getEmphasis)(data, { selectedNodeId, searchQuery }), [data, searchQuery, selectedNodeId]);
    const selectedNode = layout.nodes.find(({ id }) => id === selectedNodeId);
    const validEdgeKeys = useMemo(() => new Set(layout.edges.map(({ source, target }) => (0, graph_1.getEdgeKey)(source.id, target.id))), [layout]);
    useEffect(() => {
        const root = rootRef.current;
        if (!root) {
            return undefined;
        }
        const updateSize = ({ width, height }) => {
            setSize({
                width,
                height,
                devicePixelRatio: window.devicePixelRatio || 1,
            });
        };
        const observer = new ResizeObserver(([entry]) => updateSize(entry.contentRect));
        observer.observe(root);
        return () => observer.disconnect();
    }, []);
    useEffect(() => {
        if (fit) {
            setTransform(fit);
        }
    }, [fit]);
    useEffect(() => {
        setSelectedNodeId((current) => current && layout.nodes.some(({ id }) => id === current) ? current : undefined);
        setSelectedEdgeKeys((current) => {
            const next = new Set([...current].filter((key) => validEdgeKeys.has(key)));
            return next.size === current.size ? current : next;
        });
        setHoveredNodeId((current) => current && layout.nodes.some(({ id }) => id === current) ? current : undefined);
        setHoveredEdgeKey((current) => current && validEdgeKeys.has(current) ? current : undefined);
    }, [layout, validEdgeKeys]);
    useLayoutEffect(() => {
        const canvas = canvasRef.current;
        if (!canvas || size.width <= 0 || size.height <= 0) {
            return;
        }
        canvas.width = Math.max(1, Math.round(size.width * size.devicePixelRatio));
        canvas.height = Math.max(1, Math.round(size.height * size.devicePixelRatio));
    }, [data.nodes.length, size]);
    useEffect(() => {
        const canvas = canvasRef.current;
        if (!canvas || !transform || size.width <= 0 || size.height <= 0) {
            return undefined;
        }
        if (animationFrameRef.current !== null) {
            cancelAnimationFrame(animationFrameRef.current);
        }
        animationFrameRef.current = -1;
        const frame = requestAnimationFrame(() => {
            animationFrameRef.current = null;
            const context = canvas.getContext('2d');
            if (context) {
                (0, draw_1.drawComponentMap)(context, layout, transform, {
                    viewport: size,
                    devicePixelRatio: size.devicePixelRatio,
                    selectedNodeId,
                    hoveredEdgeKey,
                    selectedEdgeKeys,
                    emphasis,
                });
            }
        });
        if (animationFrameRef.current !== null) {
            animationFrameRef.current = frame;
        }
        return () => {
            if (animationFrameRef.current !== null) {
                cancelAnimationFrame(animationFrameRef.current);
                animationFrameRef.current = null;
            }
        };
    }, [emphasis, hoveredEdgeKey, layout, selectedEdgeKeys, selectedNodeId, size, transform]);
    const getCanvasPoint = useCallback((clientX, clientY) => {
        const bounds = canvasRef.current?.getBoundingClientRect();
        return {
            x: clientX - (bounds?.left ?? 0),
            y: clientY - (bounds?.top ?? 0),
        };
    }, []);
    const zoom = useCallback((factor, point) => {
        if (!fit) {
            return;
        }
        setTransform((current) => current
            ? (0, viewport_1.zoomAtPoint)(current, factor, point, fit.scale * 0.5, fit.scale * 8)
            : current);
    }, [fit]);
    const zoomFromToolbar = useCallback((factor) => zoom(factor, { x: size.width / 2, y: size.height / 2 }), [size, zoom]);
    const selectNode = useCallback((nodeId) => {
        if (nodeId) {
            setSearchQuery('');
        }
        setSelectedNodeId(nodeId);
        setSelectedEdgeKeys((current) => (current.size ? new Set() : current));
    }, []);
    const selectEdge = useCallback((edgeKey, additive) => {
        setSelectedEdgeKeys((current) => {
            if (!additive) {
                return new Set([edgeKey]);
            }
            const next = new Set(current);
            if (next.has(edgeKey)) {
                next.delete(edgeKey);
            }
            else {
                next.add(edgeKey);
            }
            return next;
        });
    }, []);
    const getHitTarget = useCallback((point, camera) => {
        const worldPoint = (0, viewport_1.screenToWorld)(point, camera);
        const node = (0, layout_1.hitTest)(layout, worldPoint);
        if (node) {
            return { node, edge: undefined };
        }
        const orderedEdges = (0, edgeGeometry_1.orderEdgesForInteraction)(layout.edges, hoveredEdgeKey, selectedEdgeKeys);
        return {
            node: undefined,
            edge: (0, edgeGeometry_1.hitTestEdge)(orderedEdges, worldPoint, (0, edgeGeometry_1.getEdgeHitTolerance)(camera.scale)),
        };
    }, [hoveredEdgeKey, layout, selectedEdgeKeys]);
    const updateHover = useCallback((point, camera) => {
        const { node, edge } = getHitTarget(point, camera);
        setHoveredNodeId(node?.id);
        setHoveredEdgeKey(edge ? (0, graph_1.getEdgeKey)(edge.source.id, edge.target.id) : undefined);
    }, [getHitTarget]);
    useEffect(() => {
        const canvas = canvasRef.current;
        if (!canvas) {
            return undefined;
        }
        const handleWheel = (event) => {
            event.preventDefault();
            const factor = Math.exp(-event.deltaY * WHEEL_ZOOM_SENSITIVITY);
            zoom(factor, getCanvasPoint(event.clientX, event.clientY));
        };
        canvas.addEventListener('wheel', handleWheel, NON_PASSIVE_WHEEL_OPTIONS);
        return () => canvas.removeEventListener('wheel', handleWheel, false);
    }, [data.nodes.length, getCanvasPoint, zoom]);
    const handlePointerDown = useCallback((event) => {
        if (!transform) {
            return;
        }
        const point = getCanvasPoint(event.clientX, event.clientY);
        pointerDragRef.current = {
            pointerId: event.pointerId,
            canvas: event.currentTarget,
            start: point,
            startTransform: transform,
            moved: false,
        };
        event.currentTarget.setPointerCapture(event.pointerId);
    }, [getCanvasPoint, transform]);
    const handlePointerMove = useCallback((event) => {
        const point = getCanvasPoint(event.clientX, event.clientY);
        const drag = pointerDragRef.current;
        if (!drag || drag.pointerId !== event.pointerId) {
            if (transform) {
                updateHover(point, transform);
            }
            return;
        }
        const deltaX = point.x - drag.start.x;
        const deltaY = point.y - drag.start.y;
        const moved = drag.moved || Math.hypot(deltaX, deltaY) >= CLICK_MOVEMENT_THRESHOLD;
        if (!moved) {
            return;
        }
        if (!drag.moved) {
            drag.moved = true;
            setHoveredNodeId(undefined);
            setHoveredEdgeKey(undefined);
            setDragging(true);
        }
        setTransform({
            ...drag.startTransform,
            x: drag.startTransform.x + deltaX,
            y: drag.startTransform.y + deltaY,
        });
    }, [getCanvasPoint, transform, updateHover]);
    const releasePointer = useCallback((canvas, pointerId) => {
        if (canvas.hasPointerCapture(pointerId)) {
            canvas.releasePointerCapture(pointerId);
        }
    }, []);
    const finishPointer = useCallback((event, cancelled) => {
        const drag = pointerDragRef.current;
        if (!drag || drag.pointerId !== event.pointerId) {
            return;
        }
        const point = getCanvasPoint(event.clientX, event.clientY);
        const deltaX = point.x - drag.start.x;
        const deltaY = point.y - drag.start.y;
        const moved = drag.moved || Math.hypot(deltaX, deltaY) >= CLICK_MOVEMENT_THRESHOLD;
        if (!cancelled) {
            if (moved) {
                setTransform({
                    ...drag.startTransform,
                    x: drag.startTransform.x + deltaX,
                    y: drag.startTransform.y + deltaY,
                });
            }
            else {
                const { node, edge } = getHitTarget(point, drag.startTransform);
                if (node) {
                    selectNode(node.id);
                }
                else if (edge) {
                    selectEdge((0, graph_1.getEdgeKey)(edge.source.id, edge.target.id), event.ctrlKey || event.metaKey);
                }
                else {
                    selectNode(undefined);
                }
            }
        }
        releasePointer(event.currentTarget, event.pointerId);
        pointerDragRef.current = null;
        setDragging(false);
    }, [getCanvasPoint, getHitTarget, releasePointer, selectEdge, selectNode]);
    const handleKeyDown = useCallback((event) => {
        if (event.key === 'Escape') {
            event.preventDefault();
            setSelectedNodeId(undefined);
            setSelectedEdgeKeys(new Set());
        }
    }, []);
    useEffect(() => () => {
        const drag = pointerDragRef.current;
        if (drag) {
            releasePointer(drag.canvas, drag.pointerId);
        }
        pointerDragRef.current = null;
    }, [releasePointer]);
    if (!data.nodes.length) {
        return ((0, jsx_runtime_1.jsx)("div", { ref: rootRef, className: b(), children: (0, jsx_runtime_1.jsx)("p", { className: b('empty'), children: "No significant components found" }) }));
    }
    return ((0, jsx_runtime_1.jsxs)("div", { ref: rootRef, className: b(), children: [(0, jsx_runtime_1.jsx)("canvas", { ref: canvasRef, className: b('canvas', {
                    dragging,
                    interactive: Boolean(hoveredNodeId || hoveredEdgeKey),
                }), role: "img", "aria-label": "Significant component dependency map", tabIndex: 0, onKeyDown: handleKeyDown, onPointerCancel: (event) => finishPointer(event, true), onPointerDown: handlePointerDown, onPointerLeave: () => {
                    setHoveredNodeId(undefined);
                    setHoveredEdgeKey(undefined);
                }, onPointerMove: handlePointerMove, onPointerUp: (event) => finishPointer(event, false) }), (0, jsx_runtime_1.jsxs)("div", { className: b('toolbar'), children: [(0, jsx_runtime_1.jsx)(uikit_1.TextInput, { className: b('search'), size: "l", hasClear: true, placeholder: "Search components", value: searchQuery, controlProps: { 'aria-label': 'Search components' }, onUpdate: setSearchQuery, onFocus: () => {
                            setSelectedNodeId(undefined);
                            setSelectedEdgeKeys(new Set());
                        } }), (0, jsx_runtime_1.jsx)(uikit_1.Button, { className: b('control'), size: "l", view: "outlined", "aria-label": "Zoom in", onClick: () => zoomFromToolbar(TOOLBAR_ZOOM_FACTOR), children: "+" }), (0, jsx_runtime_1.jsx)(uikit_1.Button, { className: b('control'), size: "l", view: "outlined", "aria-label": "Zoom out", onClick: () => zoomFromToolbar(1 / TOOLBAR_ZOOM_FACTOR), children: "\u2212" }), (0, jsx_runtime_1.jsx)(uikit_1.Button, { className: b('control'), size: "l", view: "outlined", "aria-label": "Fit graph", onClick: () => setTransform(fit ?? null), children: "Fit" })] }), (0, jsx_runtime_1.jsx)("output", { className: b('live'), "aria-live": "polite", children: selectedNode ? `Selected ${selectedNode.label}` : '' })] }));
}
//# sourceMappingURL=ComponentMap.js.map
