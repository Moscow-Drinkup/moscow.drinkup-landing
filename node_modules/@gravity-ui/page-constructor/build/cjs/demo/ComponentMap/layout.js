"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.KIND_ORDER = exports.WORLD_PADDING = exports.BAND_HEADER_HEIGHT = exports.GROUP_GAP = exports.VERTICAL_GAP = exports.NODE_LABEL_PADDING = exports.NODE_LABEL_FONT = exports.NODE_HEIGHT = exports.NODE_MIN_WIDTH = void 0;
exports.layoutComponentMap = layoutComponentMap;
exports.hitTest = hitTest;
const edgeGeometry_1 = require("./edgeGeometry.js");
exports.NODE_MIN_WIDTH = 208;
exports.NODE_HEIGHT = 48;
exports.NODE_LABEL_FONT = '600 13px sans-serif';
exports.NODE_LABEL_PADDING = 16;
exports.VERTICAL_GAP = 32;
exports.GROUP_GAP = 112;
exports.BAND_HEADER_HEIGHT = 36;
exports.WORLD_PADDING = 56;
exports.KIND_ORDER = ['block', 'sub-block', 'component'];
const BAND_LABELS = {
    block: 'Blocks',
    'sub-block': 'Sub-blocks',
    component: 'Components',
};
function sortByLabel(nodes) {
    return [...nodes].sort((left, right) => left.label.localeCompare(right.label));
}
function createLabelMeasurer() {
    if (typeof CanvasRenderingContext2D !== 'undefined' && typeof document !== 'undefined') {
        try {
            const context = document.createElement('canvas').getContext('2d');
            if (context) {
                context.font = exports.NODE_LABEL_FONT;
                return (label) => context.measureText(label).width;
            }
        }
        catch {
            // Use deterministic metrics when Canvas is unavailable (for example during SSR).
        }
    }
    return (label) => Array.from(label).length * 7;
}
const getNodesHeight = (count) => count === 0 ? 0 : count * exports.NODE_HEIGHT + (count - 1) * exports.VERTICAL_GAP;
function measureGroup(kind, nodes, measureLabel) {
    const sortedNodes = sortByLabel(nodes);
    const nodeWidth = Math.max(exports.NODE_MIN_WIDTH, ...sortedNodes.map(({ label }) => measureLabel(label) + exports.NODE_LABEL_PADDING * 2));
    return {
        kind,
        label: BAND_LABELS[kind],
        nodes: sortedNodes,
        nodeWidth,
        visibleHeight: exports.BAND_HEADER_HEIGHT + getNodesHeight(sortedNodes.length),
    };
}
function createBand(group, x, y) {
    return {
        kind: group.kind,
        label: group.label,
        bounds: {
            x,
            y,
            width: group.nodeWidth,
            height: group.visibleHeight,
        },
        nodes: group.nodes.map((node, index) => ({
            ...node,
            rect: {
                x,
                y: y + exports.BAND_HEADER_HEIGHT + index * (exports.NODE_HEIGHT + exports.VERTICAL_GAP),
                width: group.nodeWidth,
                height: exports.NODE_HEIGHT,
            },
        })),
    };
}
function resolveEdges(edges, nodeById) {
    return edges.map(({ source, target }) => {
        const sourceNode = nodeById.get(source);
        const targetNode = nodeById.get(target);
        if (!sourceNode || !targetNode) {
            throw new Error(`Component map edge references unknown node: ${source} -> ${target}`);
        }
        return { source: sourceNode, target: targetNode };
    });
}
function layoutComponentMap(data) {
    const nodesByKind = new Map(exports.KIND_ORDER.map((kind) => [kind, []]));
    for (const node of data.nodes) {
        nodesByKind.get(node.kind)?.push(node);
    }
    const measureLabel = createLabelMeasurer();
    const measuredGroups = exports.KIND_ORDER.map((kind) => measureGroup(kind, nodesByKind.get(kind) ?? [], measureLabel));
    const maximumGroupHeight = Math.max(...measuredGroups.map(({ visibleHeight }) => visibleHeight));
    const commonCenterY = exports.WORLD_PADDING + maximumGroupHeight / 2;
    const provisionalBands = measuredGroups.map((group) => createBand(group, 0, commonCenterY - group.visibleHeight / 2));
    const provisionalNodes = provisionalBands.flatMap(({ nodes }) => nodes);
    const provisionalNodeById = new Map(provisionalNodes.map((node) => [node.id, node]));
    const provisionalEdges = resolveEdges(data.edges, provisionalNodeById);
    const corridorByKind = new Map(exports.KIND_ORDER.map((kind) => [kind, 0]));
    for (const edge of provisionalEdges) {
        if (edge.source.kind === edge.target.kind) {
            corridorByKind.set(edge.source.kind, Math.max(corridorByKind.get(edge.source.kind) ?? 0, (0, edgeGeometry_1.getSameGroupArcOutset)(edge.source, edge.target) + edgeGeometry_1.ARROW_SIZE));
        }
    }
    let nextGroupX = exports.WORLD_PADDING;
    const bands = provisionalBands.map((band, index) => {
        const x = nextGroupX;
        const placedBand = {
            ...band,
            bounds: { ...band.bounds, x },
            nodes: band.nodes.map((node) => ({
                ...node,
                rect: { ...node.rect, x },
            })),
        };
        nextGroupX =
            x +
                band.bounds.width +
                (corridorByKind.get(band.kind) ?? 0) +
                (index === provisionalBands.length - 1 ? 0 : exports.GROUP_GAP);
        return placedBand;
    });
    const nodes = bands.flatMap((band) => band.nodes);
    const nodeById = new Map(nodes.map((node) => [node.id, node]));
    const edges = resolveEdges(data.edges, nodeById);
    const rightmostBand = bands[bands.length - 1];
    const rightmost = rightmostBand.bounds.x +
        rightmostBand.bounds.width +
        (corridorByKind.get(rightmostBand.kind) ?? 0);
    return {
        bands,
        nodes,
        edges,
        bounds: {
            x: 0,
            y: 0,
            width: rightmost + exports.WORLD_PADDING,
            height: maximumGroupHeight + exports.WORLD_PADDING * 2,
        },
    };
}
function hitTest(layoutOrNodes, point) {
    const nodes = Array.isArray(layoutOrNodes) ? layoutOrNodes : layoutOrNodes.nodes;
    for (let index = nodes.length - 1; index >= 0; index -= 1) {
        const node = nodes[index];
        const { x, y, width, height } = node.rect;
        if (point.x >= x && point.x <= x + width && point.y >= y && point.y <= y + height) {
            return node;
        }
    }
    return undefined;
}
//# sourceMappingURL=layout.js.map
