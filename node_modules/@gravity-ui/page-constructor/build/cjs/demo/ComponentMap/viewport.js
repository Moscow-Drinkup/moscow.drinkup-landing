"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.screenToWorld = exports.worldToScreen = exports.MINIMUM_VIEWPORT_SCALE = exports.VIEWPORT_PADDING = void 0;
exports.fitToViewport = fitToViewport;
exports.zoomAtPoint = zoomAtPoint;
exports.VIEWPORT_PADDING = 24;
exports.MINIMUM_VIEWPORT_SCALE = 0.01;
const worldToScreen = (point, transform) => ({
    x: point.x * transform.scale + transform.x,
    y: point.y * transform.scale + transform.y,
});
exports.worldToScreen = worldToScreen;
const screenToWorld = (point, transform) => ({
    x: (point.x - transform.x) / transform.scale,
    y: (point.y - transform.y) / transform.scale,
});
exports.screenToWorld = screenToWorld;
function fitToViewport(bounds, viewport, padding = exports.VIEWPORT_PADDING) {
    const availableWidth = Math.max(0, viewport.width - padding * 2);
    const availableHeight = Math.max(0, viewport.height - padding * 2);
    const widthScale = bounds.width > 0 ? availableWidth / bounds.width : 1;
    const heightScale = bounds.height > 0 ? availableHeight / bounds.height : 1;
    // React postpones fitting until it has a positive canvas size. Keep this pure fallback
    // invertible as well for callers that receive an empty or padding-sized viewport.
    const scale = Math.max(exports.MINIMUM_VIEWPORT_SCALE, Math.min(1, widthScale, heightScale));
    return {
        scale,
        x: viewport.width / 2 - (bounds.x + bounds.width / 2) * scale,
        y: viewport.height / 2 - (bounds.y + bounds.height / 2) * scale,
    };
}
function zoomAtPoint(transform, factor, point, minScale, maxScale) {
    const world = (0, exports.screenToWorld)(point, transform);
    const scale = Math.min(maxScale, Math.max(minScale, transform.scale * factor));
    return {
        scale,
        x: point.x - world.x * scale,
        y: point.y - world.y * scale,
    };
}
//# sourceMappingURL=viewport.js.map
