"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PALETTE = void 0;
exports.drawComponentMap = drawComponentMap;
const edgeGeometry_1 = require("./edgeGeometry.js");
const graph_1 = require("./graph.js");
const layout_1 = require("./layout.js");
exports.PALETTE = {
    bandHeading: '#4d5562',
    edge: '#7d8798',
    label: '#1f2733',
    nodeFill: {
        block: '#d9e8ff',
        'sub-block': '#e5ddff',
        component: '#e4ebf2',
    },
    nodeOutline: '#748094',
    selectedFill: '#ffbe5c',
    selectedOutline: '#9a5700',
};
const NODE_RADIUS = 10;
const NODE_DIMMED_OPACITY = 0.16;
const EDGE_DIMMED_OPACITY = 0.1;
const EDGE_WIDTH = 1.5;
const INTERACTIVE_EDGE_WIDTH = 3;
const INTERACTIVE_EDGE_COLOR = '#ffbe5c';
function roundedRect(context, x, y, width, height, radius) {
    const right = x + width;
    const bottom = y + height;
    context.beginPath();
    context.moveTo(x + radius, y);
    context.lineTo(right - radius, y);
    context.quadraticCurveTo(right, y, right, y + radius);
    context.lineTo(right, bottom - radius);
    context.quadraticCurveTo(right, bottom, right - radius, bottom);
    context.lineTo(x + radius, bottom);
    context.quadraticCurveTo(x, bottom, x, bottom - radius);
    context.lineTo(x, y + radius);
    context.quadraticCurveTo(x, y, x + radius, y);
    context.closePath();
}
function drawArrowhead(context, arrowhead) {
    context.beginPath();
    context.moveTo(arrowhead[0].x, arrowhead[0].y);
    context.lineTo(arrowhead[1].x, arrowhead[1].y);
    context.lineTo(arrowhead[2].x, arrowhead[2].y);
    context.closePath();
    context.fill();
}
function drawEdge(canvasContext, source, target, { active, hovered, selected }) {
    const context = canvasContext;
    const { start, end, firstControl, secondControl, arrowhead } = (0, edgeGeometry_1.getEdgeGeometry)(source, target);
    const color = selected || hovered ? INTERACTIVE_EDGE_COLOR : exports.PALETTE.edge;
    context.save();
    context.globalAlpha = selected || active ? 1 : EDGE_DIMMED_OPACITY;
    context.strokeStyle = color;
    context.fillStyle = color;
    context.lineWidth = selected || hovered ? INTERACTIVE_EDGE_WIDTH : EDGE_WIDTH;
    context.beginPath();
    context.moveTo(start.x, start.y);
    context.bezierCurveTo(firstControl.x, firstControl.y, secondControl.x, secondControl.y, end.x, end.y);
    context.stroke();
    drawArrowhead(context, arrowhead);
    context.restore();
}
function drawNode(canvasContext, node, active, selected) {
    const context = canvasContext;
    const { x, y, width, height } = node.rect;
    context.save();
    context.globalAlpha = active ? 1 : NODE_DIMMED_OPACITY;
    context.fillStyle = selected ? exports.PALETTE.selectedFill : exports.PALETTE.nodeFill[node.kind];
    context.strokeStyle = selected ? exports.PALETTE.selectedOutline : exports.PALETTE.nodeOutline;
    context.lineWidth = selected ? 3 : 1.5;
    roundedRect(context, x, y, width, height, NODE_RADIUS);
    context.fill();
    context.stroke();
    context.restore();
}
function drawLabel(canvasContext, node, active) {
    const context = canvasContext;
    const { x, y, width, height } = node.rect;
    context.save();
    context.globalAlpha = active ? 1 : NODE_DIMMED_OPACITY;
    context.fillStyle = exports.PALETTE.label;
    context.font = layout_1.NODE_LABEL_FONT;
    context.textAlign = 'center';
    context.textBaseline = 'middle';
    context.fillText(node.label, x + width / 2, y + height / 2);
    context.restore();
}
function drawComponentMap(canvasContext, layout, transform, options) {
    const context = canvasContext;
    const { viewport, devicePixelRatio, selectedNodeId, hoveredEdgeKey, selectedEdgeKeys, emphasis } = options;
    const hasEmphasis = emphasis.mode !== 'none';
    context.resetTransform();
    context.scale(devicePixelRatio, devicePixelRatio);
    context.clearRect(0, 0, viewport.width, viewport.height);
    context.translate(transform.x, transform.y);
    context.scale(transform.scale, transform.scale);
    context.save();
    context.fillStyle = exports.PALETTE.bandHeading;
    context.font = '700 18px sans-serif';
    context.textAlign = 'left';
    context.textBaseline = 'top';
    for (const band of layout.bands) {
        context.fillText(band.label, band.bounds.x, band.bounds.y);
    }
    context.restore();
    const edgeState = ({ source, target }) => {
        const key = (0, graph_1.getEdgeKey)(source.id, target.id);
        return {
            key,
            selected: selectedEdgeKeys.has(key),
            hovered: key === hoveredEdgeKey,
        };
    };
    const orderedEdges = (0, edgeGeometry_1.orderEdgesForInteraction)(layout.edges, hoveredEdgeKey, selectedEdgeKeys);
    for (const edge of orderedEdges) {
        const { source, target } = edge;
        const { key, selected, hovered } = edgeState(edge);
        const active = emphasis.mode === 'none' ||
            (emphasis.mode === 'selection' && emphasis.activeEdgeIds.has(key));
        drawEdge(context, source, target, { active, hovered, selected });
    }
    for (const node of layout.nodes) {
        const active = !hasEmphasis || emphasis.activeNodeIds.has(node.id);
        drawNode(context, node, active, emphasis.mode === 'selection' && node.id === selectedNodeId);
    }
    for (const node of layout.nodes) {
        const active = !hasEmphasis || emphasis.activeNodeIds.has(node.id);
        drawLabel(context, node, active);
    }
}
//# sourceMappingURL=draw.js.map
