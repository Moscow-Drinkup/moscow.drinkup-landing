"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.LangSwitcher = void 0;
const tslib_1 = require("tslib");
const jsx_runtime_1 = require("react/jsx-runtime");
const React = tslib_1.__importStar(require("react"));
const innerContext_1 = require("../../../../context/innerContext/index.js");
const models_1 = require("../../../../models/index.js");
const utils_1 = require("../../../../utils/index.js");
const b = (0, utils_1.block)('lang-switcher');
const LangSwitcher = ({ buttonText, items, image }) => {
    const [isActive, setIsActive] = React.useState(false);
    const { navItemMap } = React.useContext(innerContext_1.InnerContext);
    const navigationItems = React.useMemo(() => items.map((item) => ({
        type: models_1.NavigationItemType.Link,
        text: item.text,
        url: item.href,
    })), [items]);
    const hidePopup = React.useCallback(() => {
        setIsActive(false);
    }, []);
    const onClick = React.useCallback((event) => {
        event.stopPropagation();
        setIsActive((active) => !active);
    }, []);
    const text = buttonText ?? 'Language';
    const data = {
        type: models_1.NavigationItemType.Dropdown,
        text,
        items: navigationItems,
        isActive,
        hidePopup,
    };
    const NavigationDropdown = navItemMap[models_1.NavigationItemType.Dropdown];
    return ((0, jsx_runtime_1.jsx)(NavigationDropdown, { data: data, text: text, type: models_1.NavigationItemType.Dropdown, items: navigationItems, isActive: isActive, hidePopup: hidePopup, onClick: onClick, className: b(), placement: "top-start", icon: image, iconSize: 20 }));
};
exports.LangSwitcher = LangSwitcher;
//# sourceMappingURL=LangSwitcher.js.map
