import * as React from 'react';
import { DropdownMenuItem } from '@gravity-ui/uikit';
import { LinkProps } from "../../../models/index.js";
export type LinkItem = Required<Pick<LinkProps, 'text' | 'url'>>;
type UseOverflowListItemsProps = {
    isDropdown?: boolean;
    containerRef: React.RefObject<HTMLElement>;
    items?: LinkItem[];
    itemSelector: string;
    moreButtonWidth?: number;
};
type OverflowListItemsResult = {
    visibleItems: LinkItem[];
    hiddenItems: DropdownMenuItem[];
    measured: boolean;
};
export declare function useOverflowListItems({ isDropdown, containerRef, items, itemSelector, moreButtonWidth, }: UseOverflowListItemsProps): OverflowListItemsResult;
export {};
